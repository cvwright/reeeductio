{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"rEEEductio","text":"<p>rEEEductio is an end-to-end encrypted data layer that makes it simple to build apps with secure messaging and encrypted cloud storage \u2014 without becoming a cryptography expert.</p> <p>Early-stage project</p> <p>rEEEductio has not been independently security-audited. See the Security section for details. Use at your own risk.</p>"},{"location":"#what-it-does","title":"What it does","text":"<p>rEEEductio handles encryption, key management, authentication, and access control so you don't have to. You get:</p> <ul> <li>Encrypted messaging \u2014 end-to-end encrypted message streams, organized into topics</li> <li>Encrypted file storage \u2014 content-addressed blob storage with client-side encryption</li> <li>Encrypted key-value state \u2014 a structured store for app data and configuration</li> <li>Capability-based access control \u2014 fine-grained, signed permissions for users, roles, and bots</li> </ul> <p>The server stores only ciphertext. It never sees your data.</p>"},{"location":"#how-it-compares","title":"How it compares","text":"rEEEductio Firebase / Supabase Signal / Matrix End-to-end encrypted \u2705 \u274c \u2705 Developer SDK \u2705 \u2705 \u26a0\ufe0f Limited Self-hostable \u2705 \u274c \u2705 Open source \u2705 \u274c \u2705 No phone/email required \u2705 \u274c \u274c"},{"location":"#where-to-start","title":"Where to start","text":"<ul> <li>New here? Start with Running the Server then pick a quick start.</li> <li>Python developer? Jump to the Python Quick Start.</li> <li>TypeScript developer? Jump to the TypeScript Quick Start.</li> <li>Just browsing? Read Core Concepts for an overview.</li> </ul>"},{"location":"DOCS_PLAN/","title":"Documentation Plan","text":""},{"location":"DOCS_PLAN/#current-state-what-exists","title":"Current State: What Exists","text":"Location Content Audience <code>README.md</code> High-level overview, core concepts, security Partially developer-friendly <code>docs-internal/AUTHORIZATION.md</code> Authorization system design Internal / security expert <code>docs-internal/SECURITY.md</code> Security reference Security expert <code>docs-internal/CHAIN_VALIDATION.md</code> CAS implementation detail Backend implementer <code>docs-internal/EVENT_SOURCED_STATE.md</code> Event sourcing internals Backend implementer <code>docs-internal/UNIFIED_NAMESPACE.md</code> Capability path spec Internal spec <code>docs-internal/TYPED_IDENTIFIERS.md</code> ID encoding format Internal spec <code>python-sdk/ARCHITECTURE.md</code> SDK module breakdown SDK contributor <code>python-sdk/SECURITY.md</code> Key derivation notes Security expert <code>backend/ADMIN.md</code> Admin space API Ops / server admin <code>backend/DOCKER.md</code> Docker deployment Ops <code>backend/LOGGING.md</code> Logging config Ops"},{"location":"DOCS_PLAN/#problems-with-current-docs","title":"Problems with Current Docs","text":"<ol> <li>No Getting Started guide \u2014 zero path from \"I just discovered this\" to \"it works on my machine\"</li> <li>No tutorials or examples \u2014 no \"send your first message\" walkthrough</li> <li>README leads with cryptographic jargon \u2014 terms like HKDF, Ed25519, OPAQUE, compare-and-swap appear before developers understand what they're building</li> <li>Concepts explained from a security POV, not a developer POV \u2014 Spaces are described by their cryptographic properties rather than by what you do with them</li> <li>The old <code>docs/</code> folder is internal implementation notes, not end-user documentation</li> <li>No TypeScript SDK documentation beyond a TODO file</li> <li>No \"why this vs. alternatives\" section for developers evaluating the project</li> <li>No diagrams \u2014 the system has meaningful structure (key hierarchy, message chains, auth flow) that's hard to understand from prose alone</li> </ol>"},{"location":"DOCS_PLAN/#plan-content-to-write","title":"Plan: Content to Write","text":""},{"location":"DOCS_PLAN/#1-home-introduction-rewrite-readme-or-use-as-doc-site-home","title":"1. Home / Introduction (rewrite README or use as doc site home)","text":"<ul> <li>Plain-language pitch: \"what problem does this solve?\" (secure messaging/storage without managing crypto)</li> <li>Positioning relative to things developers know: Firebase/Supabase but zero-knowledge; like Signal/Matrix but as a library</li> <li>Brief feature list in developer terms (not security terms)</li> <li>Links to quick starts</li> </ul>"},{"location":"DOCS_PLAN/#2-getting-started-completely-new","title":"2. Getting Started (completely new)","text":"<ul> <li>Prerequisites: Python 3.10+ or Node/Bun, a running server</li> <li>Python Quick Start: Install SDK \u2192 create space \u2192 post a message \u2192 read it back (~15 lines of code)</li> <li>TypeScript Quick Start: Same workflow in TypeScript</li> <li>Running the server locally: Docker one-liner to have a server for development</li> </ul>"},{"location":"DOCS_PLAN/#3-core-concepts-developer-friendly-mostly-new","title":"3. Core Concepts (developer-friendly, mostly new)","text":"<p>Each concept explained in \"what it is / what you use it for / simple example\" format: - Spaces \u2014 the unit of deployment; think \"a project\" or \"an app instance\" - Topics \u2014 ordered message streams within a space; think \"chat channels\" or \"event logs\" - State \u2014 event-sourced key-value store; think \"configuration\" or \"member list\" - KV Data \u2014 simpler key-value for ephemeral app data - Blobs \u2014 encrypted file storage - Users &amp; Authentication \u2014 key-based identity; what this means in practice - Access Control \u2014 capabilities and roles explained without assuming familiarity with capability theory</p>"},{"location":"DOCS_PLAN/#4-how-to-guides-new","title":"4. How-To Guides (new)","text":"<p>Task-oriented, each a short focused page: - Create and configure a space - Add users to a space - Send and receive messages - Store and retrieve files - Manage roles and permissions - Set up username/password login with OPAQUE - Create a bot/tool account (e.g., an invite link) - Self-host the server</p>"},{"location":"DOCS_PLAN/#5-sdk-reference-new","title":"5. SDK Reference (new)","text":"<ul> <li>Python SDK: module-by-module reference with examples</li> <li>TypeScript SDK: same</li> </ul>"},{"location":"DOCS_PLAN/#6-server-reference-reorganize-existing","title":"6. Server Reference (reorganize existing)","text":"<ul> <li>Configuration reference (from <code>backend/DOCKER.md</code>)</li> <li>Admin API (from <code>backend/ADMIN.md</code>)</li> <li>Logging (from <code>backend/LOGGING.md</code>)</li> </ul>"},{"location":"DOCS_PLAN/#7-api-reference","title":"7. API Reference","text":"<ul> <li>Link to/embed the OpenAPI spec (already exists at <code>/docs</code> when server is running)</li> <li>Explain the ID format (from <code>docs-internal/TYPED_IDENTIFIERS.md</code>, simplified)</li> </ul>"},{"location":"DOCS_PLAN/#8-security-reference-reorganize-existing-docs-internal","title":"8. Security Reference (reorganize existing docs-internal/)","text":"<ul> <li>Move the current <code>docs-internal/</code> content here, targeted at security-conscious developers rather than implementers</li> <li>Simplified threat model and what is/isn't protected</li> <li>Honest disclosure: not audited, use at own risk</li> </ul>"},{"location":"DOCS_PLAN/#diagrams-needed","title":"Diagrams Needed","text":"Diagram Format Priority System architecture (client \u2194 server \u2194 storage) Box/arrow High Data model (Space \u2192 Topics/State/Blobs/KV) Hierarchy High Authentication flow (challenge-response \u2192 JWT) Sequence High Key derivation tree (symmetric_root \u2192 derived keys) Tree Medium Message chain (prev_hash linking) Chain Medium Capability chain of trust Tree Medium OPAQUE login flow Sequence Low <p>All diagrams should be authored in Mermaid (renders natively in MkDocs Material and on GitHub).</p>"},{"location":"DOCS_PLAN/#target-directory-structure","title":"Target Directory Structure","text":"<pre><code>docs/\n\u251c\u2500\u2500 index.md                        # Home / introduction\n\u251c\u2500\u2500 getting-started/\n\u2502   \u251c\u2500\u2500 quickstart-python.md\n\u2502   \u251c\u2500\u2500 quickstart-typescript.md\n\u2502   \u2514\u2500\u2500 running-the-server.md\n\u251c\u2500\u2500 concepts/\n\u2502   \u251c\u2500\u2500 spaces.md\n\u2502   \u251c\u2500\u2500 topics-and-messages.md\n\u2502   \u251c\u2500\u2500 state-and-data.md\n\u2502   \u251c\u2500\u2500 blobs.md\n\u2502   \u2514\u2500\u2500 access-control.md\n\u251c\u2500\u2500 how-to/\n\u2502   \u251c\u2500\u2500 create-a-space.md\n\u2502   \u251c\u2500\u2500 add-users.md\n\u2502   \u251c\u2500\u2500 send-messages.md\n\u2502   \u251c\u2500\u2500 store-files.md\n\u2502   \u251c\u2500\u2500 manage-permissions.md\n\u2502   \u251c\u2500\u2500 password-login.md\n\u2502   \u251c\u2500\u2500 tool-accounts.md\n\u2502   \u2514\u2500\u2500 self-hosting.md\n\u251c\u2500\u2500 reference/\n\u2502   \u251c\u2500\u2500 python-sdk.md\n\u2502   \u251c\u2500\u2500 typescript-sdk.md\n\u2502   \u251c\u2500\u2500 server-config.md\n\u2502   \u2514\u2500\u2500 api.md\n\u2514\u2500\u2500 security/\n    \u251c\u2500\u2500 overview.md\n    \u251c\u2500\u2500 threat-model.md\n    \u2514\u2500\u2500 internals.md\n</code></pre>"},{"location":"DOCS_PLAN/#documentation-engine-recommendation","title":"Documentation Engine Recommendation","text":""},{"location":"DOCS_PLAN/#option-a-mkdocs-material-theme-recommended","title":"Option A: MkDocs + Material theme (recommended)","text":"<ul> <li>Markdown-based, minimal config, easy to host on Read the Docs or GitHub Pages</li> <li>Material theme is the most widely used, looks professional</li> <li>Supports Mermaid diagrams natively via the <code>pymdownx.superfences</code> extension</li> <li><code>mkdocs.yml</code> config is simple; no build toolchain required</li> <li>Best if: you want lowest friction to get running</li> </ul>"},{"location":"DOCS_PLAN/#option-b-docusaurus","title":"Option B: Docusaurus","text":"<ul> <li>React/MDX-based, very polished, used by major open source projects (React, Jest, Babel)</li> <li>Excellent versioning, search, i18n support</li> <li>Better suited for multi-SDK projects (sidebar can organize Python vs TypeScript)</li> <li>Best if: you plan to invest more in docs long-term and want a best-in-class developer site</li> </ul> <p>Verdict: start with MkDocs + Material. Can migrate to Docusaurus later if the project grows.</p>"},{"location":"DOCS_PLAN/#mkdocs-setup","title":"MkDocs Setup","text":"<pre><code>pip install mkdocs-material\nmkdocs new .   # creates mkdocs.yml and docs/index.md\nmkdocs serve   # local preview at http://127.0.0.1:8000\n</code></pre> <p>Minimal <code>mkdocs.yml</code>: <pre><code>site_name: rEEEductio\ntheme:\n  name: material\n  features:\n    - navigation.tabs\n    - navigation.sections\nmarkdown_extensions:\n  - pymdownx.superfences:\n      custom_fences:\n        - name: mermaid\n          class: mermaid\n          format: !!python/name:pymdownx.superfences.fence_code_format\n  - pymdownx.highlight\n  - admonition\n</code></pre></p>"},{"location":"DOCS_PLAN/#suggested-priority-order","title":"Suggested Priority Order","text":"<ol> <li>MkDocs setup \u2014 scaffold the site so pages have a home as we write them</li> <li>Getting Started / Quick Start \u2014 biggest gap, highest impact for developer adoption</li> <li>Architecture diagram \u2014 one picture replaces 3 pages of prose</li> <li>Concepts guide \u2014 re-explains what's in README but for developers, not cryptographers</li> <li>How-to guides \u2014 practical task-oriented content</li> <li>SDK reference \u2014 detailed, can be incremental</li> <li>Reorganize <code>docs-internal/</code> content into the Security section</li> </ol>"},{"location":"TODO/","title":"Documentation TODO","text":"<p>See DOCS_PLAN.md for the full analysis and rationale.</p>"},{"location":"TODO/#infrastructure","title":"Infrastructure","text":"<ul> <li>[x] Set up MkDocs + Material theme (<code>pip install mkdocs-material</code>, create <code>mkdocs.yml</code>)</li> <li>[ ] Configure Read the Docs (or GitHub Pages) for auto-deploy</li> <li>[x] Add Mermaid diagram support to <code>mkdocs.yml</code></li> </ul>"},{"location":"TODO/#diagrams-mermaid","title":"Diagrams (Mermaid)","text":"<ul> <li>[x] System architecture \u2014 client \u2194 server \u2194 storage (<code>index.md</code>)</li> <li>[x] Data model \u2014 Space \u2192 Topics / State / Blobs / KV (<code>concepts/spaces.md</code>)</li> <li>[x] Authentication flow \u2014 challenge-response \u2192 JWT sequence diagram (<code>concepts/spaces.md</code>)</li> <li>[x] Key derivation tree \u2014 symmetric_root \u2192 message/blob/state/data keys (<code>concepts/spaces.md</code>)</li> <li>[x] Message chain \u2014 how prev_hash links messages (<code>concepts/topics-and-messages.md</code>)</li> <li>[x] Capability chain of trust \u2014 how rights flow from space creator (<code>concepts/access-control.md</code>)</li> <li>[x] OPAQUE login flow \u2014 optional password login sequence (<code>how-to/password-login.md</code>)</li> </ul>"},{"location":"TODO/#home-introduction","title":"Home / Introduction","text":"<ul> <li>[x] <code>docs/index.md</code> \u2014 plain-language pitch, comparison table, quick links (first draft done)</li> <li>[ ] Flesh out \"Why rEEEductio?\" \u2014 positioning vs. Firebase/Supabase, Signal/Matrix</li> </ul>"},{"location":"TODO/#getting-started","title":"Getting Started","text":"<ul> <li>[x] <code>getting-started/running-the-server.md</code> \u2014 Docker Compose, GHCR image, dev config</li> <li>[x] <code>getting-started/quickstart-python.md</code> \u2014 install \u2192 create space \u2192 post message \u2192 read it back</li> <li>[x] <code>getting-started/quickstart-typescript.md</code> \u2014 same flow in TypeScript (Node + browser)</li> </ul>"},{"location":"TODO/#sdk-changes-made-while-writing-docs","title":"SDK Changes Made While Writing Docs","text":"<ul> <li>[x] Fixed stale <code>C</code> space ID prefix \u2192 <code>S</code> in <code>python-sdk/ARCHITECTURE.md</code>, <code>docs-internal/TYPED_IDENTIFIERS.md</code>, <code>typescript-sdk/src/index.ts</code></li> <li>[x] Added <code>Space.decryptMessageData(msg, topicId)</code> to TypeScript SDK (<code>client.ts</code>) to match Python SDK parity</li> </ul>"},{"location":"TODO/#core-concepts","title":"Core Concepts","text":"<ul> <li>[x] <code>concepts/spaces.md</code> \u2014 what a space is, when to use it, diagram</li> <li>[x] <code>concepts/topics-and-messages.md</code> \u2014 topics, message chains, ordering guarantees</li> <li>[x] <code>concepts/state-and-data.md</code> \u2014 event-sourced state vs. lightweight KV; when to use each</li> <li>[x] <code>concepts/blobs.md</code> \u2014 content-addressed encrypted file storage</li> <li>[x] <code>concepts/access-control.md</code> \u2014 users, roles, capabilities explained for developers (not security experts)</li> </ul>"},{"location":"TODO/#how-to-guides","title":"How-To Guides","text":"<ul> <li>[x] <code>how-to/create-a-space.md</code></li> <li>[x] <code>how-to/add-users.md</code></li> <li>[x] <code>how-to/send-messages.md</code></li> <li>[x] <code>how-to/store-files.md</code></li> <li>[x] <code>how-to/manage-permissions.md</code></li> <li>[x] <code>how-to/password-login.md</code> \u2014 OPAQUE opt-in setup</li> <li>[x] <code>how-to/tool-accounts.md</code> \u2014 bots, invite links, scoped API keys</li> <li>[x] <code>how-to/self-hosting.md</code> \u2014 production deployment guide</li> </ul>"},{"location":"TODO/#sdk-reference","title":"SDK Reference","text":"<ul> <li>[x] <code>reference/python-sdk.md</code> \u2014 module-by-module reference with examples</li> <li>[x] <code>reference/typescript-sdk.md</code> \u2014 same for TypeScript SDK</li> <li>[x] <code>reference/api.md</code> \u2014 OpenAPI overview, link to <code>/docs</code> endpoint, explain ID format</li> <li>[x] <code>reference/server-config.md</code> \u2014 consolidate from <code>backend/DOCKER.md</code>, <code>backend/LOGGING.md</code>, <code>backend/ADMIN.md</code></li> </ul>"},{"location":"TODO/#security-section","title":"Security Section","text":"<ul> <li>[x] <code>security/overview.md</code> \u2014 simplified: what the system protects, what it doesn't</li> <li>[x] <code>security/threat-model.md</code> \u2014 in scope / out of scope; honest about audit status</li> <li>[x] <code>security/internals.md</code> \u2014 reorganize <code>docs-internal/</code> content for security-curious developers</li> </ul>"},{"location":"concepts/access-control/","title":"Access Control","text":"<p>rEEEductio uses a capability-based permission system. Instead of a fixed set of admin/member roles, you define exactly what each user or bot is allowed to do \u2014 and those permissions are enforced cryptographically, not just checked at the API layer.</p>"},{"location":"concepts/access-control/#key-concepts","title":"Key concepts","text":""},{"location":"concepts/access-control/#space-creator","title":"Space creator","text":"<p>The creator of a space (the entity whose Ed25519 public key is encoded in the space ID) has implicit full access to everything in the space. You don't need to grant yourself permissions \u2014 you're the root of trust.</p>"},{"location":"concepts/access-control/#users","title":"Users","text":"<p>Every human participant in a space is identified by their user ID (<code>U...</code>). Users authenticate with an Ed25519 private key and can be granted permissions directly or through roles.</p>"},{"location":"concepts/access-control/#roles","title":"Roles","text":"<p>A role is a named collection of capabilities (e.g. <code>\"member\"</code>, <code>\"moderator\"</code>, <code>\"viewer\"</code>). You define roles yourself \u2014 there are no built-in role names. Once a role exists, you assign it to users and attach capabilities to it.</p>"},{"location":"concepts/access-control/#capabilities","title":"Capabilities","text":"<p>A capability is a single permission: one operation on one resource path. Every capability has:</p> Field Description <code>op</code> What the holder can do: <code>read</code>, <code>create</code>, <code>modify</code>, <code>delete</code>, or <code>write</code> <code>path</code> Which resource it applies to (supports wildcards) <code>must_be_owner</code> Optional: restricts to the holder's own resources only <p>Operations: - <code>read</code> \u2014 read-only - <code>create</code> \u2014 write, only if the object does not yet exist - <code>modify</code> \u2014 write, only if the object already exists - <code>delete</code> \u2014 delete an existing object - <code>write</code> \u2014 full write access (implies create, modify, delete)</p> <p>Path wildcards: - <code>{any}</code> \u2014 matches exactly one path segment (e.g. <code>topics/{any}</code> matches <code>topics/general</code>) - <code>{...}</code> \u2014 matches any number of segments at any depth - <code>{self}</code> \u2014 matches the authenticated user's own ID (useful for \"each user can only write their own profile\")</p>"},{"location":"concepts/access-control/#tool-accounts","title":"Tool accounts","text":"<p>Tools are bot or service accounts. They authenticate just like users but have no ambient authority \u2014 every operation must be covered by an explicit capability. This makes them safe to use for automation without risking accidental privilege escalation.</p>"},{"location":"concepts/access-control/#how-authorization-works","title":"How authorization works","text":"<pre><code>flowchart LR\n    R[Request] --&gt; CC{Is space creator?}\n    CC --&gt;|Yes| A[Allow]\n    CC --&gt;|No| LC[Load user capabilities]\n    LC --&gt; DC[Direct capabilities on user]\n    LC --&gt; RC[Capabilities via assigned roles]\n    DC &amp; RC --&gt; CE{Capability\\nmatches\\nrequest?}\n    CE --&gt;|Yes| A\n    CE --&gt;|No| D[Deny]</code></pre> <p>All authorization state (users, roles, capabilities) is stored in the space's State store under <code>auth/</code>, and every entry must carry a valid cryptographic signature. The server verifies the chain of trust before accepting any change.</p>"},{"location":"concepts/access-control/#adding-users-to-a-space","title":"Adding users to a space","text":""},{"location":"concepts/access-control/#direct-addition-admin","title":"Direct addition (admin)","text":"<p>If you have full access to the space, you can add a user directly by their user ID:</p> <pre><code># Python SDK / CLI\nspace.add_user(user_id='U...')\nspace.create_role('member')\nspace.assign_role_to_user(user_id='U...', role_name='member')\n</code></pre>"},{"location":"concepts/access-control/#invitation-links-self-registration","title":"Invitation links (self-registration)","text":"<p>Generate a one-time invitation token that lets a new user add themselves:</p> <pre><code># Creator generates invitation\ninvite = space.create_invitation()\n# Share invite.private_key with the invitee out-of-band\n\n# Invitee uses the invitation to join\nspace.accept_invitation(invite_private_key=..., new_user_id='U...')\n</code></pre> <p>The invitation is a scoped tool keypair with only the capability to create one user entry. After use, it's effectively expired (the user entry already exists, so <code>create</code> would be rejected again).</p>"},{"location":"concepts/access-control/#defining-roles-and-capabilities","title":"Defining roles and capabilities","text":"<pre><code># Create a role\nspace.create_role('moderator')\n\n# Grant capabilities to the role\nspace.grant_capability_to_role(\n    role_name='moderator',\n    cap_id='read-all-topics',\n    capability={'op': 'read', 'path': 'topics/{any}'},\n)\nspace.grant_capability_to_role(\n    role_name='moderator',\n    cap_id='delete-any-message',\n    capability={'op': 'delete', 'path': 'topics/{any}/messages/{any}'},\n)\n\n# Assign role to a user\nspace.assign_role_to_user(user_id='U...', role_name='moderator')\n</code></pre>"},{"location":"concepts/access-control/#example-read-only-guest-role","title":"Example: read-only guest role","text":"<pre><code># Create a viewer role\nspace.create_role('viewer')\n\n# Let viewers read all topics\nspace.grant_capability_to_role(\n    role_name='viewer',\n    cap_id='read-topics',\n    capability={'op': 'read', 'path': 'topics/{any}'},\n)\n\n# Invite a guest\ninvite = space.create_invitation()\n# ... share invite key with guest ...\n# When they join, assign them the viewer role\nspace.assign_role_to_user(user_id='U...', role_name='viewer')\n</code></pre>"},{"location":"concepts/access-control/#example-user-private-data","title":"Example: user-private data","text":"<p>The <code>{self}</code> wildcard lets you give each user write access to only their own data:</p> <pre><code>space.grant_capability_to_role(\n    role_name='member',\n    cap_id='own-profile',\n    capability={'op': 'write', 'path': 'state/profiles/{self}'},\n)\n</code></pre> <p>A user with this capability can write to <code>state/profiles/{their_own_id}</code> but not to any other profile path.</p>"},{"location":"concepts/access-control/#state-paths-used-by-the-authorization-system","title":"State paths used by the authorization system","text":"<p>All auth state lives under the <code>auth/</code> prefix and follows this layout:</p> <pre><code>auth/\n\u251c\u2500\u2500 users/{user_id}                     \u2190 user entry\n\u2502   \u251c\u2500\u2500 roles/{role_name}               \u2190 role assignment\n\u2502   \u2514\u2500\u2500 rights/{cap_id}                 \u2190 direct capability\n\u251c\u2500\u2500 roles/{role_name}                   \u2190 role definition\n\u2502   \u2514\u2500\u2500 rights/{cap_id}                 \u2190 role's capability\n\u2514\u2500\u2500 tools/{tool_id}                     \u2190 tool entry\n    \u2514\u2500\u2500 rights/{cap_id}                 \u2190 tool's capability\n</code></pre> <p>You generally don't need to interact with these paths directly \u2014 the SDK methods handle it. But understanding the layout helps when debugging permissions or building advanced tooling.</p>"},{"location":"concepts/access-control/#related-concepts","title":"Related concepts","text":"<ul> <li>Spaces \u2014 access control is per-space</li> <li>State &amp; Data \u2014 auth state is stored in the space's State store</li> <li>Topics &amp; Messages \u2014 topic read/write is controlled by capabilities</li> </ul>"},{"location":"concepts/blobs/","title":"Blobs","text":"<p>Blobs are rEEEductio's encrypted file storage. Use them whenever you need to store binary data that's too large for a message payload \u2014 images, documents, audio files, backups, and so on.</p>"},{"location":"concepts/blobs/#how-blobs-work","title":"How blobs work","text":"<p>Blob storage is content-addressed: the blob's ID is derived from its content. Upload the same bytes twice, and you get the same blob ID back.</p> <p>Encryption happens entirely client-side. The SDK encrypts the file before uploading, and decrypts after downloading. The server and object store (MinIO by default) only ever see ciphertext.</p> <pre><code>sequenceDiagram\n    participant C as Client\n    participant S as Server\n    participant O as Object Store (MinIO/S3)\n\n    C-&gt;&gt;C: Encrypt file with blob key\n    C-&gt;&gt;S: POST /spaces/{id}/blobs (metadata)\n    S--&gt;&gt;C: {blob_id, upload_url}\n    C-&gt;&gt;O: PUT upload_url (encrypted bytes)\n    note over C,O: Server never sees plaintext\n\n    C-&gt;&gt;S: GET /spaces/{id}/blobs/{blob_id}/download\n    S--&gt;&gt;C: {download_url}\n    C-&gt;&gt;O: GET download_url (encrypted bytes)\n    C-&gt;&gt;C: Decrypt with blob key</code></pre>"},{"location":"concepts/blobs/#blob-ids","title":"Blob IDs","text":"<p>Each blob gets a <code>B</code>-prefixed content-addressed ID (e.g. <code>Babc123...</code>). This ID is stable: the same plaintext content always produces the same blob ID, regardless of when or how many times you upload it.</p>"},{"location":"concepts/blobs/#uploading-a-blob","title":"Uploading a blob","text":"PythonTypeScript <pre><code># Read a file and upload it\nwith open('photo.jpg', 'rb') as f:\n    image_data = f.read()\n\nresult = space.upload_encrypted_blob(\n    data=image_data,\n    content_type='image/jpeg',\n)\nprint(result.blob_id)   # B...\n\n# Save the blob ID in a message so others can find it\nspace.post_encrypted_message(\n    topic_id='photos',\n    message_type='chat.image',\n    data=result.blob_id.encode(),\n)\n</code></pre> <pre><code>import { stringToBytes } from 'reeeductio';\n\n// Read a file (e.g. from a browser &lt;input type=\"file\"&gt;)\nconst arrayBuffer = await file.arrayBuffer();\nconst data = new Uint8Array(arrayBuffer);\n\nconst result = await space.encryptAndUploadBlob(data, file.type);\nconsole.log(result.blob_id);   // B...\n\n// Post the blob ID as a message so others can find it\nawait space.postEncryptedMessage(\n  'photos',\n  'chat.image',\n  stringToBytes(result.blob_id),\n);\n</code></pre>"},{"location":"concepts/blobs/#downloading-a-blob","title":"Downloading a blob","text":"PythonTypeScript <pre><code># Download and decrypt\ndata = space.download_and_decrypt_blob(blob_id='B...')\nwith open('downloaded.jpg', 'wb') as f:\n    f.write(data)\n</code></pre> <pre><code>const data = await space.downloadAndDecryptBlob('B...');\n// data is a Uint8Array containing the original file bytes\n</code></pre>"},{"location":"concepts/blobs/#deleting-a-blob","title":"Deleting a blob","text":"PythonTypeScript <pre><code>space.delete_blob(blob_id='B...')\n</code></pre> <pre><code>await space.deleteBlob('B...');\n</code></pre> <p>Deletion is permanent</p> <p>Once a blob is deleted, it cannot be recovered. If any messages reference the blob ID, those references will be broken.</p>"},{"location":"concepts/blobs/#encryption-details","title":"Encryption details","text":"<p>Blob encryption uses the same HKDF key hierarchy as the rest of the space. A blob-specific key is derived from <code>symmetricRoot</code>. You do not need to manage blob keys manually \u2014 the SDK handles it.</p>"},{"location":"concepts/blobs/#linking-blobs-and-messages","title":"Linking blobs and messages","text":"<p>The convention for sharing a blob in a conversation is to post a message whose <code>data</code> payload is the blob ID. Recipients see the message, extract the blob ID, and download and decrypt the blob separately.</p> <p>This keeps messages small while allowing arbitrarily large file attachments.</p>"},{"location":"concepts/blobs/#related-concepts","title":"Related concepts","text":"<ul> <li>Topics &amp; Messages \u2014 post blob IDs as message payloads</li> <li>Spaces \u2014 blobs are scoped to a single space</li> <li>State &amp; Data \u2014 for small structured values, not large files</li> </ul>"},{"location":"concepts/spaces/","title":"Spaces","text":"<p>A space is the top-level container in rEEEductio. Think of it as a secure, encrypted workspace \u2014 like a group chat room, a shared project folder, or a per-user data silo, depending on how you use it.</p> <p>Everything in rEEEductio lives inside a space: messages, files, structured state, and key-value data. Access to a space is controlled by cryptographic keys, not passwords or API tokens.</p>"},{"location":"concepts/spaces/#what-a-space-contains","title":"What a space contains","text":"<pre><code>graph TD\n    S[\"Space (S...)\"]\n    S --&gt; T[\"Topics (message chains)\"]\n    S --&gt; ST[\"State (event-sourced paths)\"]\n    S --&gt; B[\"Blobs (encrypted files)\"]\n    S --&gt; D[\"Data (key-value pairs)\"]\n    S --&gt; U[\"Members (users &amp; tools)\"]</code></pre> Component What it is Typical use Topics Ordered chains of encrypted messages Chat, event streams, audit logs State Named paths with version history User profiles, config, access control Blobs Content-addressed encrypted files Attachments, images, backups Data Lightweight signed key-value pairs Ephemeral metadata, flags Members Users and tool accounts with roles Multi-user collaboration"},{"location":"concepts/spaces/#space-identity","title":"Space identity","text":"<p>Every space has two complementary keys:</p> <p>Ed25519 key pair \u2014 the signing identity. The public key is encoded into the space ID and user ID. The private key proves who you are to the server.</p> <p>Symmetric root \u2014 a 32-byte random secret shared among the space's members. The SDK uses it with HKDF to derive separate encryption keys for messages, state, and blobs, without any single key doing double-duty.</p> <pre><code>graph TD\n    SR[\"Symmetric Root (32 random bytes)\"]\n    SR --&gt;|HKDF| MK[\"message_key (per space)\"]\n    SR --&gt;|HKDF| DK[\"data_key (per space)\"]\n    MK --&gt;|HKDF| TK1[\"topic_key['general']\"]\n    MK --&gt;|HKDF| TK2[\"topic_key['alerts']\"]\n    MK --&gt;|HKDF| SK[\"state_key (reserved topic 'state')\"]\n    TK1 --&gt;|AES-GCM| E1[Encrypted messages in 'general']\n    TK2 --&gt;|AES-GCM| E2[Encrypted messages in 'alerts']\n    SK  --&gt;|AES-GCM| E3[Encrypted state entries]\n    DK  --&gt;|AES-GCM| E4[Encrypted data entries]</code></pre> <p>Each topic has its own key, derived fresh each time from <code>message_key</code>. Sharing a topic key with someone does not expose any other topic's data.</p> <p>Neither the key pair nor the symmetric root alone is enough. You need both to read and write data in a space.</p>"},{"location":"concepts/spaces/#authentication-flow","title":"Authentication flow","text":"<p>Every API request is authenticated with a short-lived JWT token. The token is obtained through a challenge-response exchange using your Ed25519 private key \u2014 no passwords involved.</p> <pre><code>sequenceDiagram\n    participant C as Client (SDK)\n    participant S as Server\n\n    C-&gt;&gt;S: POST /spaces/{spaceId}/auth/challenge {public_key: \"U...\"}\n    S--&gt;&gt;C: {challenge: \"&lt;random string&gt;\", expires_at: ...}\n    C-&gt;&gt;C: signature = Ed25519.sign(challenge, privateKey)\n    C-&gt;&gt;S: POST /spaces/{spaceId}/auth/verify {public_key, challenge, signature}\n    S-&gt;&gt;S: Verify signature against public key\n    S--&gt;&gt;C: {token: \"&lt;JWT&gt;\", expires_at: ...}\n    C-&gt;&gt;S: GET /spaces/{spaceId}/topics/general/messages Authorization: Bearer &lt;JWT&gt;\n    S--&gt;&gt;C: [{message_hash, data, ...}, ...]</code></pre> <p>The SDK handles this automatically \u2014 you never need to call the auth endpoints directly.</p>"},{"location":"concepts/spaces/#space-and-user-ids","title":"Space and user IDs","text":"<p>Both IDs are derived from the same Ed25519 public key, just with a different type prefix:</p> ID Starts with Purpose Space ID <code>S</code> Identifies the space to the server User ID <code>U</code> Identifies you as a member of the space <p>Both are 44-character URL-safe base64 strings.</p>"},{"location":"concepts/spaces/#the-space-creator","title":"The space creator","text":"<p>When you create a new space, your key pair becomes the first and only member. You are the space creator \u2014 the implicit admin with full rights to invite others and grant permissions.</p> <p>With <code>auto_create_spaces: true</code> in the server config (default for local dev), the space is created automatically the first time you connect using a new space ID. You don't need any separate \"create space\" API call.</p>"},{"location":"concepts/spaces/#spaces-and-servers","title":"Spaces and servers","text":"<p>A space lives on a single server. The server stores encrypted ciphertext and enforces access control (who can read/write which parts of the space), but it never sees plaintext \u2014 encryption and decryption happen exclusively on the client.</p> <p>You can think of the server as a trusted courier: it delivers messages reliably and in order, but it cannot read them.</p>"},{"location":"concepts/spaces/#when-to-use-multiple-spaces","title":"When to use multiple spaces","text":"<p>One space per logical boundary of trust. Good rules of thumb:</p> <ul> <li>One space per user \u2014 each user's private data in its own space that only they can access.</li> <li>One space per team or project \u2014 shared among the team; each member has the <code>symmetric_root</code>.</li> <li>One space per integration \u2014 bot accounts or third-party tools get their own space (or a scoped tool account inside an existing space).</li> </ul> <p>Spaces are cheap to create. Don't try to share one space across unrelated trust domains.</p>"},{"location":"concepts/spaces/#creating-a-space","title":"Creating a space","text":"PythonTypeScript <pre><code>import os\nfrom reeeductio.crypto import generate_keypair, to_space_id, to_user_id\n\nprivate_key, public_key = generate_keypair()\nsymmetric_root = os.urandom(32)\n\nspace_id = to_space_id(public_key)   # starts with 'S'\nuser_id  = to_user_id(public_key)    # starts with 'U'\n</code></pre> <pre><code>import { generateKeyPair, toSpaceId, toUserId } from 'reeeductio';\n\nconst keyPair = await generateKeyPair();\nconst symmetricRoot = crypto.getRandomValues(new Uint8Array(32));\n\nconst spaceId = toSpaceId(keyPair.publicKey);   // starts with 'S'\nconst userId  = toUserId(keyPair.publicKey);    // starts with 'U'\n</code></pre>"},{"location":"concepts/spaces/#related-concepts","title":"Related concepts","text":"<ul> <li>Topics &amp; Messages \u2014 how ordered message chains work inside a space</li> <li>Access Control \u2014 how to invite users and grant roles</li> <li>State &amp; Data \u2014 structured storage beyond messages</li> </ul>"},{"location":"concepts/state-and-data/","title":"State &amp; Data","text":"<p>Beyond messages, rEEEductio provides two lighter-weight storage primitives: State and Data. Both live inside a space, both are encrypted, and both are designed for structured metadata rather than conversation streams.</p>"},{"location":"concepts/state-and-data/#state-event-sourced-paths","title":"State \u2014 event-sourced paths","text":"<p>State is a key-value store where each path has a history. Every write creates a new entry in an append-only event log, so you can always look back at what a value was at any point in time.</p> <p>Think of it like a Git repository for your configuration: you see the current value at HEAD, but the full history is always there.</p>"},{"location":"concepts/state-and-data/#what-state-is-good-for","title":"What state is good for","text":"<ul> <li>User profiles and preferences</li> <li>Access control lists (who has what role)</li> <li>Application configuration</li> <li>Audit logs of setting changes</li> </ul>"},{"location":"concepts/state-and-data/#state-paths","title":"State paths","text":"<p>Paths use a slash-separated hierarchy (e.g. <code>profiles/alice</code>, <code>config/feature-flags</code>). The current value at a path is the most recent write to that path.</p> <p>Note</p> <p>State paths are different from topic IDs. They can contain slashes and are not subject to the same character restrictions as topic names.</p>"},{"location":"concepts/state-and-data/#reading-and-writing-state","title":"Reading and writing state","text":"PythonTypeScript <pre><code># Store a user profile (plaintext)\nspace.set_plaintext_state('profiles/alice', '{\"name\": \"Alice\", \"role\": \"admin\"}')\n\n# Read it back\nprofile_json = space.get_plaintext_state('profiles/alice')\nprint(profile_json)  # {\"name\": \"Alice\", \"role\": \"admin\"}\n</code></pre> <pre><code>import { stringToBytes, bytesToString } from 'reeeductio';\n\n// Store a user profile (plaintext)\nawait space.setPlaintextState('profiles/alice', stringToBytes('{\"name\": \"Alice\", \"role\": \"admin\"}'));\n\n// Read it back\nconst profileBytes = await space.getPlaintextState('profiles/alice');\nconsole.log(bytesToString(profileBytes));\n</code></pre>"},{"location":"concepts/state-and-data/#encrypted-state","title":"Encrypted state","text":"<p>For sensitive values (tokens, private settings), use the encrypted variants. The SDK uses <code>stateKey</code> (derived from <code>symmetricRoot</code>) to encrypt client-side before sending:</p> PythonTypeScript <pre><code># Store encrypted\nspace.set_encrypted_state('secrets/api-key', b'my-secret-value')\n\n# Read and decrypt\nvalue = space.get_encrypted_state('secrets/api-key')\n</code></pre> <pre><code>// Store encrypted\nawait space.setEncryptedState('secrets/api-key', stringToBytes('my-secret-value'));\n\n// Read and decrypt\nconst value = await space.getEncryptedState('secrets/api-key');\n</code></pre>"},{"location":"concepts/state-and-data/#state-history","title":"State history","text":"<p>Since state is event-sourced, you can retrieve the full change history:</p> PythonTypeScript <pre><code>history = space.get_state_history()\nfor entry in history.messages:\n    print(entry.path, entry.server_timestamp)\n</code></pre> <pre><code>const history = await space.getStateHistory();\nfor (const entry of history.messages) {\n  console.log(entry.path, entry.server_timestamp);\n}\n</code></pre>"},{"location":"concepts/state-and-data/#data-lightweight-key-value-pairs","title":"Data \u2014 lightweight key-value pairs","text":"<p>Data is a simpler, flat key-value store. Each entry is a single value at a path, with no history. It's optimized for values that change frequently and where you only ever care about the latest value.</p> <p>Each data entry is signed by the writer, so you can verify authenticity, but it is not chained \u2014 there's no <code>prev_hash</code>.</p>"},{"location":"concepts/state-and-data/#what-data-is-good-for","title":"What data is good for","text":"<ul> <li>Presence and availability status</li> <li>Ephemeral metadata (e.g. \"user is typing\")</li> <li>Counters and cursors</li> <li>Short-lived configuration flags</li> </ul>"},{"location":"concepts/state-and-data/#reading-and-writing-data","title":"Reading and writing data","text":"PythonTypeScript <pre><code># Set a value\nspace.set_data('presence/alice', b'online')\n\n# Read it back\nentry = space.get_data('presence/alice')\nprint(entry.value)\n</code></pre> <pre><code>import { stringToBytes } from 'reeeductio';\n\n// Set a value\nawait space.setData('presence/alice', stringToBytes('online'));\n\n// Read it back\nconst entry = await space.getData('presence/alice');\n</code></pre>"},{"location":"concepts/state-and-data/#state-vs-data-which-to-use","title":"State vs. Data \u2014 which to use?","text":"State Data History Full history preserved Latest value only Integrity Hash-chained, tamper-evident Signed, no chain Encryption Optional (plaintext or encrypted) Signed; encryption optional Best for Config, profiles, audit logs Presence, ephemeral metadata <p>When in doubt: if you might ever want to know what the value was at some point in the past, use State. If you only care about the current value, use Data.</p>"},{"location":"concepts/state-and-data/#related-concepts","title":"Related concepts","text":"<ul> <li>Spaces \u2014 State and Data both live inside a space</li> <li>Topics &amp; Messages \u2014 for ordered event streams and chat</li> <li>Blobs \u2014 for large binary payloads</li> </ul>"},{"location":"concepts/topics-and-messages/","title":"Topics &amp; Messages","text":"<p>Topics are named, ordered channels within a space. Every message belongs to exactly one topic. You can have as many topics as you like \u2014 <code>\"general\"</code>, <code>\"alerts\"</code>, <code>\"notifications\"</code> \u2014 and each maintains its own independent message chain.</p> <p>Topic IDs must be 2\u201364 characters of lowercase letters, digits, hyphens, or underscores, starting and ending with an alphanumeric character (e.g. <code>general</code>, <code>user-notes</code>, <code>audit_log</code>).</p>"},{"location":"concepts/topics-and-messages/#the-message-chain","title":"The message chain","text":"<p>Messages in a topic form a hash chain: each message records the hash of the previous one. This gives you a tamper-evident, append-only log. If any message is altered after the fact, every subsequent hash would be wrong, and the SDK detects it.</p> <pre><code>sequenceDiagram\n    participant C as Client\n    participant S as Server\n\n    C-&gt;&gt;S: POST /spaces/{id}/topics/{topic}/messages&lt;br/&gt;{type, data, prev_hash, sender, signature}\n    S--&gt;&gt;C: {message_hash, server_timestamp}\n    C-&gt;&gt;S: GET /spaces/{id}/topics/{topic}/messages\n    S--&gt;&gt;C: [{message_hash, prev_hash, data, ...}, ...]\n    C-&gt;&gt;C: Validate chain &amp; decrypt</code></pre> <p>Each message on the wire looks like:</p> Field Description <code>message_hash</code> <code>M</code>-prefixed hash of this message's content <code>prev_hash</code> Hash of the previous message (or <code>null</code> for the first) <code>type</code> Application-defined string, e.g. <code>\"chat.text\"</code>, <code>\"file.upload\"</code> <code>data</code> Base64-encoded AES-GCM ciphertext <code>sender</code> User ID of the author <code>signature</code> Ed25519 signature over the message content <code>server_timestamp</code> When the server received the message (milliseconds UTC)"},{"location":"concepts/topics-and-messages/#encryption","title":"Encryption","text":"<p>All message data is encrypted before it leaves the client. The server stores only ciphertext.</p> <p>Key derivation follows this tree:</p> <pre><code>symmetricRoot\n\u2514\u2500\u2500 message_key  (HKDF \"message key | {spaceId}\")\n    \u2514\u2500\u2500 topic_key  (HKDF \"topic key | {topicId}\")\n</code></pre> <p>Each topic has its own derived key, so one topic's messages cannot be decrypted with another topic's key.</p>"},{"location":"concepts/topics-and-messages/#sending-a-message","title":"Sending a message","text":"PythonTypeScript <pre><code>from reeeductio import Space\n\nspace = Space(space_id=..., member_id=..., private_key=..., symmetric_root=...)\n\nresult = space.post_encrypted_message(\n    topic_id='general',\n    message_type='chat.text',\n    data=b'Hello, encrypted world!',\n)\nprint(result.message_hash)   # M...\n</code></pre> <pre><code>import { Space, stringToBytes } from 'reeeductio';\n\nconst space = new Space({ spaceId, keyPair, symmetricRoot, baseUrl });\n\nconst result = await space.postEncryptedMessage(\n  'general',\n  'chat.text',\n  stringToBytes('Hello, encrypted world!'),\n);\nconsole.log(result.message_hash);  // M...\n</code></pre>"},{"location":"concepts/topics-and-messages/#reading-messages","title":"Reading messages","text":"PythonTypeScript <pre><code>msgs = space.get_messages('general')\nfor msg in msgs.messages:\n    plaintext = space.decrypt_message_data(msg, 'general')\n    print(plaintext.decode())\n</code></pre> <pre><code>const { messages } = await space.getMessages('general');\nfor (const msg of messages) {\n  const plaintext = space.decryptMessageData(msg, 'general');\n  console.log(new TextDecoder().decode(plaintext));\n}\n</code></pre>"},{"location":"concepts/topics-and-messages/#message-ordering","title":"Message ordering","text":"<p>Messages are returned in the order the server received them (by <code>server_timestamp</code>). The SDK's chain validation independently verifies that the <code>prev_hash</code> links are correct \u2014 so you always get a consistent, ordered, tamper-evident log regardless of whether you trust the server's timestamp.</p> <p>You can query by time range:</p> PythonTypeScript <pre><code>import time\n\none_hour_ago = int(time.time() * 1000) - 3_600_000\nmsgs = space.get_messages('general', from_ts=one_hour_ago)\n</code></pre> <pre><code>const oneHourAgo = Date.now() - 3_600_000;\nconst { messages } = await space.getMessages('general', { from: oneHourAgo });\n</code></pre>"},{"location":"concepts/topics-and-messages/#message-types","title":"Message types","text":"<p>The <code>type</code> field is a free-form string you define for your application. A simple convention is <code>\"category.action\"</code>:</p> Type Meaning <code>chat.text</code> Plain text chat message <code>chat.image</code> Image attachment (data holds a blob ID) <code>file.upload</code> File was uploaded <code>event.join</code> User joined <p>The server doesn't interpret or filter by type \u2014 that's entirely up to your application.</p>"},{"location":"concepts/topics-and-messages/#real-time-updates-websocket","title":"Real-time updates (WebSocket)","text":"<p>You can subscribe to a topic's live stream over WebSocket:</p> PythonTypeScript <pre><code>async with space.subscribe('general') as stream:\n    async for msg in stream:\n        plaintext = space.decrypt_message_data(msg, 'general')\n        print(plaintext.decode())\n</code></pre> <pre><code>// Coming soon \u2014 see the reference for the low-level WebSocket API\n</code></pre>"},{"location":"concepts/topics-and-messages/#related-concepts","title":"Related concepts","text":"<ul> <li>Spaces \u2014 the container that holds topics</li> <li>Access Control \u2014 who can post to a topic</li> <li>Blobs \u2014 for large file payloads referenced from messages</li> </ul>"},{"location":"getting-started/quickstart-python/","title":"Python Quick Start","text":"<p>This guide walks you through sending your first encrypted message with the Python SDK. It assumes you have a server running locally \u2014 see Running the Server if you haven't done that yet.</p>"},{"location":"getting-started/quickstart-python/#install-the-sdk","title":"Install the SDK","text":"<pre><code>pip install reeeductio\n</code></pre>"},{"location":"getting-started/quickstart-python/#create-a-space-and-send-a-message","title":"Create a space and send a message","text":"<p>A space is a self-contained encrypted environment. To create one you generate a keypair \u2014 the private key is the space's root credential. The holder of that key is the space's first (and initially only) member.</p> <p>Save the following as <code>quickstart.py</code> and run it:</p> <pre><code>import json\nimport os\nfrom reeeductio import Space, generate_keypair\n\n# --- Step 1: Generate credentials ---\n# The space keypair is the root identity for this space.\n# The symmetric_root is the shared secret used to derive all encryption keys.\n# Save both somewhere safe \u2014 you need them to reconnect to this space.\nspace_keypair = generate_keypair()\nsymmetric_root = os.urandom(32)\n\nspace_id  = space_keypair.to_space_id()   # starts with 'S'\nmember_id = space_keypair.to_user_id()    # starts with 'U'\n\nprint(f\"Space ID:  {space_id}\")\nprint(f\"Member ID: {member_id}\")\n\n# Save credentials so you can reconnect later\ncreds = {\n    \"space_id\": space_id,\n    \"member_id\": member_id,\n    \"private_key_hex\": space_keypair.private_key.hex(),\n    \"symmetric_root_hex\": symmetric_root.hex(),\n}\nwith open(\"space_creds.json\", \"w\") as f:\n    json.dump(creds, f, indent=2)\nprint(\"Credentials saved to space_creds.json\")\n\n# --- Step 2: Connect ---\n# With auto_create_spaces enabled on the server, the space is created automatically\n# the first time you connect to it.\nwith Space(\n    space_id=space_id,\n    member_id=member_id,\n    private_key=space_keypair.private_key,\n    symmetric_root=symmetric_root,\n    base_url=\"http://localhost:8000\",\n) as space:\n\n    # --- Step 3: Post an encrypted message ---\n    # Messages are encrypted client-side before leaving your machine.\n    # The server never sees plaintext.\n    result = space.post_encrypted_message(\n        topic_id=\"general\",\n        msg_type=\"chat.text\",\n        data=b\"Hello, encrypted world!\",\n    )\n    print(f\"\\nPosted message: {result.message_hash}\")\n\n    # --- Step 4: Read it back ---\n    messages = space.get_messages(\"general\")\n    print(f\"\\nMessages in 'general' ({len(messages)} total):\")\n    for msg in messages:\n        plaintext = space.decrypt_message_data(msg, \"general\")\n        print(f\"  [{msg.message_hash[:12]}...] {plaintext.decode()}\")\n</code></pre> <p>Run it:</p> <pre><code>python quickstart.py\n</code></pre> <p>You should see output like:</p> <pre><code>Space ID:  S...\nMember ID: U...\nCredentials saved to space_creds.json\n\nPosted message: M...\n\nMessages in 'general' (1 total):\n  [Mxyz123abc456...] Hello, encrypted world!\n</code></pre>"},{"location":"getting-started/quickstart-python/#reconnecting-to-an-existing-space","title":"Reconnecting to an existing space","text":"<p>Your credentials are in <code>space_creds.json</code>. Load them to reconnect:</p> <pre><code>import json\nfrom reeeductio import Space\n\nwith open(\"space_creds.json\") as f:\n    creds = json.load(f)\n\nwith Space(\n    space_id=creds[\"space_id\"],\n    member_id=creds[\"member_id\"],\n    private_key=bytes.fromhex(creds[\"private_key_hex\"]),\n    symmetric_root=bytes.fromhex(creds[\"symmetric_root_hex\"]),\n    base_url=\"http://localhost:8000\",\n) as space:\n    messages = space.get_messages(\"general\")\n    for msg in messages:\n        plaintext = space.decrypt_message_data(msg, \"general\")\n        print(plaintext.decode())\n</code></pre> <p>Keep your credentials safe</p> <p>The <code>private_key</code> and <code>symmetric_root</code> are the keys to your space. Anyone who has them can read and write everything in it. Never commit them to source control.</p>"},{"location":"getting-started/quickstart-python/#what-just-happened","title":"What just happened?","text":"<ul> <li>Key generation: <code>generate_keypair()</code> created an Ed25519 keypair. The private key   authenticates you to the server; the public key becomes your space ID (<code>S...</code>) and   user ID (<code>U...</code>).</li> <li>Key derivation: The <code>symmetric_root</code> is used with HKDF to derive separate   encryption keys for messages, state, blobs, and data \u2014 without you managing any of that.</li> <li>Client-side encryption: <code>post_encrypted_message()</code> encrypted your message with   AES-GCM before sending it. The server stored only ciphertext.</li> <li>Chain integrity: Each message records the hash of the previous one, so tampering   or reordering is detectable.</li> </ul>"},{"location":"getting-started/quickstart-python/#next-steps","title":"Next steps","text":"<ul> <li>Add more users to your space</li> <li>Store encrypted files</li> <li>Explore the full Python SDK reference</li> </ul>"},{"location":"getting-started/quickstart-typescript/","title":"TypeScript Quick Start","text":"<p>This guide walks you through sending your first encrypted message with the TypeScript SDK. It assumes you have a server running locally \u2014 see Running the Server if you haven't done that yet.</p> <p>The SDK works in both Node.js (\u226518) and the browser.</p>"},{"location":"getting-started/quickstart-typescript/#install-the-sdk","title":"Install the SDK","text":"<pre><code>npm install reeeductio\n</code></pre>"},{"location":"getting-started/quickstart-typescript/#create-a-space-and-send-a-message","title":"Create a space and send a message","text":"<p>Save the following as <code>quickstart.ts</code> and run it:</p> <pre><code>import { writeFileSync } from 'node:fs';\nimport {\n  Space,\n  generateKeyPair,\n  toSpaceId,\n  toUserId,\n  stringToBytes,\n  bytesToString,\n} from 'reeeductio';\n\n// --- Step 1: Generate credentials ---\n// The key pair is the root identity for this space.\n// The symmetricRoot is the shared secret used to derive all encryption keys.\n// Save both somewhere safe \u2014 you need them to reconnect to this space.\nconst keyPair = await generateKeyPair();\nconst symmetricRoot = crypto.getRandomValues(new Uint8Array(32));\n\nconst spaceId = toSpaceId(keyPair.publicKey);   // starts with 'S'\nconst userId  = toUserId(keyPair.publicKey);    // starts with 'U'\n\nconsole.log('Space ID:         ', spaceId);\nconsole.log('User ID:          ', userId);\nconsole.log('Private key (hex):', Buffer.from(keyPair.privateKey).toString('hex'));\nconsole.log('Symmetric root:   ', Buffer.from(symmetricRoot).toString('hex'));\n\n// Save credentials so you can reconnect later\nconst creds = {\n  spaceId,\n  privateKeyHex: Buffer.from(keyPair.privateKey).toString('hex'),\n  publicKeyHex:  Buffer.from(keyPair.publicKey).toString('hex'),\n  symmetricRootHex: Buffer.from(symmetricRoot).toString('hex'),\n};\nwriteFileSync('space_creds.json', JSON.stringify(creds, null, 2));\nconsole.log('\\nCredentials saved to space_creds.json');\n\n// --- Step 2: Connect ---\n// With auto_create_spaces enabled on the server, the space is created\n// automatically the first time you connect to it.\nconst space = new Space({\n  spaceId,\n  keyPair,\n  symmetricRoot,\n  baseUrl: 'http://localhost:8000',\n});\n\n// --- Step 3: Post an encrypted message ---\n// Messages are encrypted client-side before leaving your machine.\n// The server never sees plaintext.\nconst topicId = 'general';\nconst result = await space.postEncryptedMessage(\n  topicId,\n  'chat.text',\n  stringToBytes('Hello, encrypted world!')\n);\nconsole.log('\\nPosted message:', result.message_hash);\n\n// --- Step 4: Read it back ---\nconst { messages } = await space.getMessages(topicId);\nconsole.log(`\\nMessages in '${topicId}' (${messages.length} total):`);\n\nfor (const msg of messages) {\n  const plaintext = bytesToString(space.decryptMessageData(msg, topicId));\n  console.log(`  [${msg.message_hash.slice(0, 12)}...] ${plaintext}`);\n}\n</code></pre> <p>Run it with Node.js (no compile step needed for <code>.ts</code> with modern tooling):</p> tsxNode.js --experimental-strip-typesCompile first <pre><code>npx tsx quickstart.ts\n</code></pre> <pre><code>node --experimental-strip-types quickstart.ts\n</code></pre> <pre><code>npx tsc quickstart.ts --module nodenext --moduleresolution nodenext\nnode quickstart.js\n</code></pre> <p>You should see output like:</p> <pre><code>Space ID:          S...\nUser ID:           U...\nPrivate key (hex): ...\nSymmetric root:    ...\n\nCredentials saved to space_creds.json\n\nPosted message: M...\n\nMessages in 'general' (1 total):\n  [Mxyz123abc456...] Hello, encrypted world!\n</code></pre>"},{"location":"getting-started/quickstart-typescript/#reconnecting-to-an-existing-space","title":"Reconnecting to an existing space","text":"<p>Your credentials are in <code>space_creds.json</code>. Load them to reconnect:</p> <pre><code>import { readFileSync } from 'node:fs';\nimport { Space, bytesToString } from 'reeeductio';\n\nconst creds = JSON.parse(readFileSync('space_creds.json', 'utf8'));\n\nconst keyPair = {\n  privateKey: Uint8Array.from(Buffer.from(creds.privateKeyHex, 'hex')),\n  publicKey:  Uint8Array.from(Buffer.from(creds.publicKeyHex,  'hex')),\n};\nconst symmetricRoot = Uint8Array.from(Buffer.from(creds.symmetricRootHex, 'hex'));\n\nconst space = new Space({\n  spaceId: creds.spaceId,\n  keyPair,\n  symmetricRoot,\n  baseUrl: 'http://localhost:8000',\n});\n\nconst topicId = 'general';\nconst { messages } = await space.getMessages(topicId);\n\nfor (const msg of messages) {\n  const plaintext = bytesToString(space.decryptMessageData(msg, topicId));\n  console.log(plaintext);\n}\n</code></pre> <p>Keep your credentials safe</p> <p>The <code>privateKey</code> and <code>symmetricRoot</code> are the keys to your space. Anyone who has them can read and write everything in it. Never commit them to source control.</p>"},{"location":"getting-started/quickstart-typescript/#using-the-sdk-in-the-browser","title":"Using the SDK in the browser","text":"<p>The SDK has no Node.js dependencies and works in modern browsers as-is. Swap out the <code>node:fs</code> credential-saving code for <code>localStorage</code> or your app's preferred storage, and everything else is identical:</p> <pre><code>import { Space, generateKeyPair, toSpaceId, stringToBytes } from 'reeeductio';\n\nconst keyPair = await generateKeyPair();\nconst symmetricRoot = crypto.getRandomValues(new Uint8Array(32));\n\n// Store credentials in localStorage (use a proper secure store in production)\nlocalStorage.setItem('privateKey', btoa(String.fromCharCode(...keyPair.privateKey)));\nlocalStorage.setItem('symmetricRoot', btoa(String.fromCharCode(...symmetricRoot)));\n\nconst space = new Space({\n  spaceId: toSpaceId(keyPair.publicKey),\n  keyPair,\n  symmetricRoot,\n  baseUrl: 'http://localhost:8000',\n});\n\nawait space.postEncryptedMessage('general', 'chat.text', stringToBytes('Hello from the browser!'));\n</code></pre>"},{"location":"getting-started/quickstart-typescript/#what-just-happened","title":"What just happened?","text":"<ul> <li>Key generation: <code>generateKeyPair()</code> created an Ed25519 keypair. The public key   is encoded into your space ID (<code>S...</code>) and user ID (<code>U...</code>).</li> <li>Key derivation: <code>symmetricRoot</code> is used with HKDF to derive separate encryption   keys per topic, state, blobs, and data \u2014 without you managing any of that.</li> <li>Client-side encryption: <code>postEncryptedMessage()</code> encrypted your message with   AES-GCM before sending it. The server stored only ciphertext.</li> <li>Decryption: <code>space.decryptMessageData(msg, topicId)</code> derives the topic key and   decrypts locally \u2014 the server is never involved in decryption.</li> </ul>"},{"location":"getting-started/quickstart-typescript/#next-steps","title":"Next steps","text":"<ul> <li>Add more users to your space</li> <li>Store encrypted files</li> <li>Explore the full TypeScript SDK reference</li> </ul>"},{"location":"getting-started/running-the-server/","title":"Running the Server","text":"<p>The quickest way to get a rEEEductio server running locally is with Docker Compose. You'll need Docker Desktop (or Docker Engine + Compose plugin) installed.</p>"},{"location":"getting-started/running-the-server/#1-create-a-working-directory","title":"1. Create a working directory","text":"<pre><code>mkdir my-reeeductio &amp;&amp; cd my-reeeductio\n</code></pre>"},{"location":"getting-started/running-the-server/#2-create-a-config-file","title":"2. Create a config file","text":"<p>Create <code>config/config.yaml</code> with the following content:</p> <pre><code>environment: development\ndebug: true\n\nserver:\n  host: 0.0.0.0\n  port: 8000\n\ndatabase:\n  type: sqlite\n\nblob_store:\n  type: filesystem\n  path: /data/blobs\n\nlogging:\n  level: INFO\n  format: text\n\n# Skip admin space setup \u2014 any client can create spaces directly.\n# Use this for local development only.\nadmin:\n  auto_create_spaces: true\n</code></pre> <p>Development only</p> <p><code>auto_create_spaces: true</code> lets anyone create a space on your server without authentication. This is fine for local development but should never be used in production. See Self-Hosting for a production configuration.</p>"},{"location":"getting-started/running-the-server/#3-create-a-compose-file","title":"3. Create a Compose file","text":"<p>Create <code>docker-compose.yml</code>:</p> <pre><code>services:\n  reeeductio:\n    image: ghcr.io/cvwright/reeeductio:latest\n    ports:\n      - \"8000:8000\"\n    volumes:\n      - ./data:/data\n      - ./config:/config\n    environment:\n      - CONFIG_FILE=/config/config.yaml\n    restart: unless-stopped\n</code></pre>"},{"location":"getting-started/running-the-server/#4-start-the-server","title":"4. Start the server","text":"<pre><code>docker compose up -d\n</code></pre> <p>Docker will pull the image on first run. Once it's up, verify the server is healthy:</p> <pre><code>curl http://localhost:8000/health\n</code></pre> <p>You should see a <code>200 OK</code> response.</p>"},{"location":"getting-started/running-the-server/#5-browse-the-api","title":"5. Browse the API","text":"<p>The server serves interactive API documentation at:</p> <pre><code>http://localhost:8000/docs\n</code></pre> <p>Open that in your browser to explore the available endpoints.</p>"},{"location":"getting-started/running-the-server/#stopping-the-server","title":"Stopping the server","text":"<pre><code>docker compose down\n</code></pre> <p>Your data is persisted in the <code>./data</code> directory, so it will still be there when you start again.</p>"},{"location":"getting-started/running-the-server/#next-steps","title":"Next steps","text":"<p>Pick a quick start to start building with the server you just set up:</p> <ul> <li>Python Quick Start</li> <li>TypeScript Quick Start</li> </ul>"},{"location":"how-to/add-users/","title":"Add Users","text":"<p>This guide covers how to add other users to an existing space, assign them roles, and remove them when needed.</p>"},{"location":"how-to/add-users/#prerequisites","title":"Prerequisites","text":"<ul> <li>You are the space creator (or have <code>write</code> capability on <code>auth/users/{any}</code>).</li> <li>You have the new user's user ID (<code>U...</code>). They generate this from their own Ed25519 keypair.</li> </ul>"},{"location":"how-to/add-users/#how-users-get-their-user-id","title":"How users get their user ID","text":"<p>Each user generates their own keypair independently. Their user ID is the public key encoded with a <code>U</code> prefix:</p> PythonTypeScript <pre><code>import os\nfrom reeeductio.crypto import generate_keypair, to_user_id, to_space_id\n\nprivate_key, public_key = generate_keypair()\nsymmetric_root = os.urandom(32)   # will be shared with them by the space creator\n\nuser_id  = to_user_id(public_key)   # share this with the space creator\nspace_id = to_space_id(public_key)  # their own space ID (for their private space)\nprint('My user ID:', user_id)\n</code></pre> <pre><code>import { generateKeyPair, toUserId } from 'reeeductio';\n\nconst keyPair = await generateKeyPair();\nconst userId = toUserId(keyPair.publicKey);\nconsole.log('My user ID:', userId);  // share this with the space creator\n</code></pre>"},{"location":"how-to/add-users/#adding-a-user-space-creator-admin","title":"Adding a user (space creator / admin)","text":""},{"location":"how-to/add-users/#step-1-register-the-user-in-the-space","title":"Step 1 \u2014 Register the user in the space","text":"PythonCLI <pre><code>from reeeductio import Space\n\nspace = Space(\n    space_id='S...',\n    member_id='U...',   # your own user ID\n    private_key=...,\n    symmetric_root=...,\n    base_url='http://localhost:8000',\n)\n\n# Add the new user\nspace.add_user('U...')   # their user ID\n</code></pre> <pre><code>reeeductio-admin user add U... \\\n    --space-key &lt;your-private-key-hex&gt; \\\n    --symmetric-root &lt;symmetric-root-hex&gt;\n</code></pre>"},{"location":"how-to/add-users/#step-2-share-the-symmetric-root","title":"Step 2 \u2014 Share the symmetric root","text":"<p>The <code>symmetric_root</code> is the shared secret that unlocks encryption for the space. Send it to the new user over a secure channel (e.g. Signal, an encrypted email, or in-person).</p> <p>Share the symmetric root securely</p> <p>Anyone who has the <code>symmetric_root</code> can decrypt all content in the space. Never send it in plaintext over an untrusted channel.</p>"},{"location":"how-to/add-users/#step-3-assign-a-role-optional-but-recommended","title":"Step 3 \u2014 Assign a role (optional but recommended)","text":"<p>Without a role, a newly added user has no capabilities and can't read or write anything. Assign them a role:</p> PythonCLI <pre><code># First create the role if it doesn't exist yet\nspace.create_role('member')\nspace.grant_capability_to_role('member', 'read-topics', {'op': 'read', 'path': 'topics/{any}'})\nspace.grant_capability_to_role('member', 'post-topics', {'op': 'create', 'path': 'topics/{any}/messages/{any}'})\n\n# Then assign it\nspace.assign_role_to_user('U...', 'member')\n</code></pre> <pre><code># Create role and grant capabilities\nreeeductio-admin role create member\nreeeductio-admin role grant member --cap-id read-topics --op read --path \"topics/{any}\"\nreeeductio-admin role grant member --cap-id post-topics --op create --path \"topics/{any}/messages/{any}\"\n\n# Assign to user\nreeeductio-admin user assign-role U... --role member\n</code></pre>"},{"location":"how-to/add-users/#the-new-user-connects","title":"The new user connects","text":"<p>Once added, the new user connects using the space ID and the shared <code>symmetric_root</code>, plus their own keypair:</p> PythonTypeScript <pre><code>from reeeductio import Space\n\nspace = Space(\n    space_id='S...',          # provided by the space creator\n    member_id='U...',         # their own user ID\n    private_key=...,          # their own private key\n    symmetric_root=...,       # shared by the space creator\n    base_url='http://localhost:8000',\n)\nmsgs = space.get_messages('general')\n</code></pre> <pre><code>import { Space } from 'reeeductio';\n\nconst space = new Space({\n  spaceId: 'S...',          // provided by the space creator\n  keyPair,                  // their own key pair\n  symmetricRoot,            // shared by the space creator\n  baseUrl: 'http://localhost:8000',\n});\nconst { messages } = await space.getMessages('general');\n</code></pre>"},{"location":"how-to/add-users/#removing-a-user","title":"Removing a user","text":"<p>Removing a user revokes their authorization state. They will no longer be able to authenticate with the server.</p> PythonCLI <pre><code># Set their auth entry to empty (marks as removed)\nspace.set_plaintext_state(f'auth/users/{user_id}', '')\n</code></pre> <pre><code>reeeductio-admin user remove U...\n</code></pre> <p>Encryption keys</p> <p>Removing a user does not rotate the <code>symmetric_root</code>. They still have the copy you gave them. If you need to ensure a removed user cannot decrypt future messages, rotate the space's encryption keys by creating a new space and migrating members.</p>"},{"location":"how-to/add-users/#granting-direct-capabilities-without-a-role","title":"Granting direct capabilities (without a role)","text":"<p>For one-off permissions, you can grant a capability directly to a user:</p> PythonCLI <pre><code>space.grant_capability_to_user(\n    user_id='U...',\n    cap_id='read-audit-log',\n    capability={'op': 'read', 'path': 'topics/audit-log'},\n)\n</code></pre> <pre><code>reeeductio-admin user grant U... \\\n    --cap-id read-audit-log \\\n    --op read \\\n    --path \"topics/audit-log\"\n</code></pre>"},{"location":"how-to/add-users/#related","title":"Related","text":"<ul> <li>Manage Permissions \u2014 in-depth roles and capabilities</li> <li>Tool Accounts \u2014 add bots and service accounts</li> <li>Access Control \u2014 concept overview</li> </ul>"},{"location":"how-to/create-a-space/","title":"Create a Space","text":"<p>This guide walks through generating credentials, connecting to a space for the first time, and saving credentials so you can reconnect later.</p>"},{"location":"how-to/create-a-space/#prerequisites","title":"Prerequisites","text":"<ul> <li>The rEEEductio server is running locally. See Running the Server if you haven't done that.</li> <li>The server is configured with <code>auto_create_spaces: true</code> (the default dev config). This lets the space be created automatically on first connect.</li> </ul>"},{"location":"how-to/create-a-space/#1-generate-credentials","title":"1. Generate credentials","text":"<p>A space is identified by an Ed25519 keypair and a 32-byte symmetric root. Generate both:</p> PythonTypeScript <pre><code>import os\nimport json\nfrom reeeductio.crypto import generate_keypair, to_space_id, to_user_id\n\nprivate_key, public_key = generate_keypair()\nsymmetric_root = os.urandom(32)\n\nspace_id = to_space_id(public_key)\nuser_id  = to_user_id(public_key)\n\nprint('Space ID:       ', space_id)\nprint('User ID:        ', user_id)\nprint('Private key:    ', private_key.hex())\nprint('Symmetric root: ', symmetric_root.hex())\n\n# Save to a file for later\ncreds = {\n    'space_id': space_id,\n    'user_id': user_id,\n    'private_key_hex': private_key.hex(),\n    'symmetric_root_hex': symmetric_root.hex(),\n}\nwith open('space_creds.json', 'w') as f:\n    json.dump(creds, f, indent=2)\nprint('Saved to space_creds.json')\n</code></pre> <pre><code>import { writeFileSync } from 'node:fs';\nimport { generateKeyPair, toSpaceId, toUserId } from 'reeeductio';\n\nconst keyPair = await generateKeyPair();\nconst symmetricRoot = crypto.getRandomValues(new Uint8Array(32));\n\nconst spaceId = toSpaceId(keyPair.publicKey);\nconst userId  = toUserId(keyPair.publicKey);\n\nconsole.log('Space ID:       ', spaceId);\nconsole.log('User ID:        ', userId);\nconsole.log('Private key:    ', Buffer.from(keyPair.privateKey).toString('hex'));\nconsole.log('Symmetric root: ', Buffer.from(symmetricRoot).toString('hex'));\n\nconst creds = {\n  spaceId,\n  userId,\n  privateKeyHex:     Buffer.from(keyPair.privateKey).toString('hex'),\n  publicKeyHex:      Buffer.from(keyPair.publicKey).toString('hex'),\n  symmetricRootHex:  Buffer.from(symmetricRoot).toString('hex'),\n};\nwriteFileSync('space_creds.json', JSON.stringify(creds, null, 2));\nconsole.log('Saved to space_creds.json');\n</code></pre> <p>Keep your credentials safe</p> <p>Anyone with your <code>private_key</code> and <code>symmetric_root</code> has full access to your space. Never commit <code>space_creds.json</code> to source control.</p>"},{"location":"how-to/create-a-space/#2-connect-first-time","title":"2. Connect (first time)","text":"<p>With <code>auto_create_spaces: true</code>, the server creates the space automatically when you first authenticate:</p> PythonTypeScript <pre><code>from reeeductio import Space\n\nspace = Space(\n    space_id=space_id,\n    member_id=user_id,\n    private_key=private_key,\n    symmetric_root=symmetric_root,\n    base_url='http://localhost:8000',\n)\n# Authentication happens automatically on first API call\nmsgs = space.get_messages('general')\nprint(f'Space ready. Messages in general: {len(msgs)}')\n</code></pre> <pre><code>import { Space } from 'reeeductio';\n\nconst space = new Space({ spaceId, keyPair, symmetricRoot, baseUrl: 'http://localhost:8000' });\nconst { messages } = await space.getMessages('general');\nconsole.log(`Space ready. Messages in general: ${messages.length}`);\n</code></pre>"},{"location":"how-to/create-a-space/#3-reconnect-from-saved-credentials","title":"3. Reconnect from saved credentials","text":"PythonTypeScript <pre><code>import json\nfrom reeeductio import Space\n\nwith open('space_creds.json') as f:\n    creds = json.load(f)\n\nspace = Space(\n    space_id=creds['space_id'],\n    member_id=creds['user_id'],\n    private_key=bytes.fromhex(creds['private_key_hex']),\n    symmetric_root=bytes.fromhex(creds['symmetric_root_hex']),\n    base_url='http://localhost:8000',\n)\n</code></pre> <pre><code>import { readFileSync } from 'node:fs';\nimport { Space } from 'reeeductio';\n\nconst creds = JSON.parse(readFileSync('space_creds.json', 'utf8'));\n\nconst keyPair = {\n  privateKey: Uint8Array.from(Buffer.from(creds.privateKeyHex, 'hex')),\n  publicKey:  Uint8Array.from(Buffer.from(creds.publicKeyHex,  'hex')),\n};\nconst symmetricRoot = Uint8Array.from(Buffer.from(creds.symmetricRootHex, 'hex'));\n\nconst space = new Space({\n  spaceId: creds.spaceId,\n  keyPair,\n  symmetricRoot,\n  baseUrl: 'http://localhost:8000',\n});\n</code></pre>"},{"location":"how-to/create-a-space/#without-auto_create_spaces","title":"Without auto_create_spaces","text":"<p>If the server does not have <code>auto_create_spaces: true</code>, an admin must register the space first using the admin CLI. See Self-Hosting for production setup details.</p>"},{"location":"how-to/create-a-space/#next-steps","title":"Next steps","text":"<ul> <li>Add Users \u2014 invite other people to your space</li> <li>Send Messages \u2014 post encrypted messages to topics</li> <li>Manage Permissions \u2014 set up roles and capabilities</li> </ul>"},{"location":"how-to/manage-permissions/","title":"Manage Permissions","text":"<p>This guide covers creating roles, defining capabilities, assigning roles to users, and revoking access.</p> <p>For a conceptual overview, see Access Control.</p>"},{"location":"how-to/manage-permissions/#overview","title":"Overview","text":"<p>Permissions flow like this:</p> <ol> <li>Create a role (e.g. <code>\"member\"</code>, <code>\"moderator\"</code>)</li> <li>Grant capabilities to the role (what operations it can perform on which paths)</li> <li>Assign the role to users</li> </ol> <p>You can also grant capabilities directly to a user without going through a role.</p>"},{"location":"how-to/manage-permissions/#creating-a-role","title":"Creating a role","text":"PythonCLI <pre><code>from reeeductio import Space\n\nspace = Space(\n    space_id='S...',\n    member_id='U...',\n    private_key=...,\n    symmetric_root=...,\n    base_url='http://localhost:8000',\n)\n\nspace.create_role('member')\nspace.create_role('moderator', description='Can delete messages and manage topics')\n</code></pre> <pre><code>reeeductio-admin role create member\nreeeductio-admin role create moderator --description \"Can delete messages and manage topics\"\n</code></pre>"},{"location":"how-to/manage-permissions/#granting-capabilities-to-a-role","title":"Granting capabilities to a role","text":"<p>A capability specifies an <code>op</code> (operation) and a <code>path</code> (resource). The path supports wildcards.</p>"},{"location":"how-to/manage-permissions/#available-operations","title":"Available operations","text":"Op Meaning <code>read</code> Read-only <code>create</code> Write, only if the resource does not already exist <code>modify</code> Write, only if the resource already exists <code>delete</code> Delete an existing resource <code>write</code> Full write (create + modify + delete)"},{"location":"how-to/manage-permissions/#path-wildcards","title":"Path wildcards","text":"Wildcard Matches <code>{any}</code> Exactly one path segment <code>{...}</code> Any number of segments <code>{self}</code> The authenticated user's own ID"},{"location":"how-to/manage-permissions/#examples","title":"Examples","text":"PythonCLI <pre><code># Read all topics\nspace.grant_capability_to_role(\n    role_name='member',\n    cap_id='read-all-topics',\n    capability={'op': 'read', 'path': 'topics/{any}'},\n)\n\n# Post to all topics\nspace.grant_capability_to_role(\n    role_name='member',\n    cap_id='post-all-topics',\n    capability={'op': 'create', 'path': 'topics/{any}/messages/{any}'},\n)\n\n# Read and write own profile only\nspace.grant_capability_to_role(\n    role_name='member',\n    cap_id='own-profile',\n    capability={'op': 'write', 'path': 'state/profiles/{self}'},\n)\n\n# Moderator can delete any message\nspace.grant_capability_to_role(\n    role_name='moderator',\n    cap_id='delete-messages',\n    capability={'op': 'delete', 'path': 'topics/{any}/messages/{any}'},\n)\n</code></pre> <pre><code>reeeductio-admin role grant member \\\n    --cap-id read-all-topics \\\n    --op read \\\n    --path \"topics/{any}\"\n\nreeeductio-admin role grant member \\\n    --cap-id post-all-topics \\\n    --op create \\\n    --path \"topics/{any}/messages/{any}\"\n\nreeeductio-admin role grant member \\\n    --cap-id own-profile \\\n    --op write \\\n    --path \"state/profiles/{self}\"\n\nreeeductio-admin role grant moderator \\\n    --cap-id delete-messages \\\n    --op delete \\\n    --path \"topics/{any}/messages/{any}\"\n</code></pre>"},{"location":"how-to/manage-permissions/#assigning-a-role-to-a-user","title":"Assigning a role to a user","text":"PythonCLI <pre><code>space.assign_role_to_user('U...', 'member')\nspace.assign_role_to_user('U...', 'moderator')  # can have multiple roles\n</code></pre> <pre><code>reeeductio-admin user assign-role U... --role member\nreeeductio-admin user assign-role U... --role moderator\n</code></pre>"},{"location":"how-to/manage-permissions/#granting-a-capability-directly-to-a-user","title":"Granting a capability directly to a user","text":"<p>For one-off permissions that don't fit a role:</p> PythonCLI <pre><code>space.grant_capability_to_user(\n    user_id='U...',\n    cap_id='read-audit',\n    capability={'op': 'read', 'path': 'topics/audit-log'},\n)\n</code></pre> <pre><code>reeeductio-admin user grant U... \\\n    --cap-id read-audit \\\n    --op read \\\n    --path \"topics/audit-log\"\n</code></pre>"},{"location":"how-to/manage-permissions/#revoking-a-role-or-capability","title":"Revoking a role or capability","text":"<p>Roles and capabilities are stored as state entries. To revoke, set the entry to empty:</p> Python <pre><code># Revoke a role assignment\nspace.set_plaintext_state(f'auth/users/{user_id}/roles/member', '')\n\n# Revoke a direct capability\nspace.set_plaintext_state(f'auth/users/{user_id}/rights/read-audit', '')\n</code></pre>"},{"location":"how-to/manage-permissions/#common-permission-patterns","title":"Common permission patterns","text":""},{"location":"how-to/manage-permissions/#read-only-guest","title":"Read-only guest","text":"<pre><code>space.create_role('guest')\nspace.grant_capability_to_role('guest', 'read-topics', {'op': 'read', 'path': 'topics/{any}'})\n</code></pre>"},{"location":"how-to/manage-permissions/#full-member-read-post-not-delete","title":"Full member (read + post, not delete)","text":"<pre><code>space.create_role('member')\nspace.grant_capability_to_role('member', 'read-topics', {'op': 'read', 'path': 'topics/{any}'})\nspace.grant_capability_to_role('member', 'post-topics', {'op': 'create', 'path': 'topics/{any}/messages/{any}'})\nspace.grant_capability_to_role('member', 'own-profile', {'op': 'write', 'path': 'state/profiles/{self}'})\n</code></pre>"},{"location":"how-to/manage-permissions/#moderator-read-post-delete-messages","title":"Moderator (read + post + delete messages)","text":"<pre><code>space.create_role('moderator')\nspace.grant_capability_to_role('moderator', 'all-topics', {'op': 'write', 'path': 'topics/{any}'})\n</code></pre>"},{"location":"how-to/manage-permissions/#service-account-write-only-to-one-topic","title":"Service account (write-only to one topic)","text":"<pre><code>space.create_tool('T...')\nspace.grant_capability_to_tool('T...', 'post-alerts', {'op': 'create', 'path': 'topics/alerts/messages/{any}'})\n</code></pre> <p>See Tool Accounts for the full guide on service accounts.</p>"},{"location":"how-to/manage-permissions/#related","title":"Related","text":"<ul> <li>Access Control \u2014 concept overview</li> <li>Add Users \u2014 adding users before assigning roles</li> <li>Tool Accounts \u2014 permissions for bots and services</li> </ul>"},{"location":"how-to/password-login/","title":"Password Login (OPAQUE)","text":"<p>By default, rEEEductio uses public-key authentication \u2014 you authenticate by signing a challenge with your Ed25519 private key. There are no passwords.</p> <p>OPAQUE is an optional add-on that lets users protect their credentials with a password. It does not replace the keypair \u2014 instead, it lets you recover your private key and symmetric root from a password, so you don't have to store them in plaintext.</p> <p>Optional feature</p> <p>OPAQUE requires one-time setup by the space admin. If you just want to get started, skip this page \u2014 the default keypair authentication works fine.</p>"},{"location":"how-to/password-login/#what-opaque-is-and-isnt","title":"What OPAQUE is (and isn't)","text":"<p>OPAQUE is an asymmetric Password Authenticated Key Exchange (aPAKE) protocol. In rEEEductio, it is used exclusively for credential recovery, not for access control.</p> <ul> <li>Your Ed25519 keypair is still randomly generated, not derived from your password.</li> <li>Your password is never sent to the server, not even hashed.</li> <li>If you change your password, your public key and space ID stay the same.</li> <li>OPAQUE lets you reconstruct your <code>privateKey</code> and <code>symmetricRoot</code> from a password, which you can then use for the normal challenge-response login.</li> </ul>"},{"location":"how-to/password-login/#opaque-login-flow","title":"OPAQUE login flow","text":"<pre><code>sequenceDiagram\n    participant U as User (browser)\n    participant S as Server\n\n    note over U,S: Registration (once, when setting up password)\n    U-&gt;&gt;U: Generate random keypair + symmetricRoot\n    U-&gt;&gt;U: OPAQUE registration init (client-side)\n    U-&gt;&gt;S: POST /opaque/register/init\n    S--&gt;&gt;U: OPAQUE registration response\n    U-&gt;&gt;U: OPAQUE registration finish \u2192 export_key\n    U-&gt;&gt;U: Wrap (privateKey + symmetricRoot) with export_key\n    U-&gt;&gt;S: POST /opaque/register/finish\\n(stores encrypted credentials on server)\n\n    note over U,S: Login (subsequent visits)\n    U-&gt;&gt;U: OPAQUE login init (client-side)\n    U-&gt;&gt;S: POST /opaque/login/init\n    S--&gt;&gt;U: OPAQUE login response\n    U-&gt;&gt;U: OPAQUE login finish \u2192 export_key\n    U-&gt;&gt;U: Unwrap (privateKey + symmetricRoot) from export_key\n    U-&gt;&gt;S: POST /spaces/{spaceId}/auth/challenge\n    S--&gt;&gt;U: challenge\n    U-&gt;&gt;U: Sign challenge with recovered privateKey\n    U-&gt;&gt;S: POST /spaces/{spaceId}/auth/verify\n    S--&gt;&gt;U: JWT token</code></pre> <p>The key insight: the server never sees your password. It only sees OPAQUE protocol messages from which the password cannot be recovered.</p>"},{"location":"how-to/password-login/#setup-enable-opaque-for-a-space","title":"Setup: enable OPAQUE for a space","text":"<p>OPAQUE requires one-time admin setup before any user can register. The space admin runs:</p> PythonTypeScript <pre><code>from reeeductio import Space\nfrom reeeductio.opaque import enable_opaque\n\nspace = Space(space_id=..., member_id=..., private_key=..., symmetric_root=...)\nenable_opaque(space)\n</code></pre> <pre><code>import { Space } from 'reeeductio';\n\nconst space = new Space({ spaceId, keyPair, symmetricRoot, baseUrl });\nawait space.enableOpaque();\n</code></pre> <p>This generates an OPAQUE server setup and stores it in the space's data store. It only needs to be done once.</p>"},{"location":"how-to/password-login/#registering-a-password","title":"Registering a password","text":"<p>Once OPAQUE is enabled, any member can register a password:</p> PythonTypeScript <pre><code>from reeeductio.opaque import perform_opaque_registration\n\nperform_opaque_registration(\n    space=space,\n    username='alice',\n    password='my-secure-password',\n)\n</code></pre> <pre><code>await space.performOpaqueRegistration('alice', 'my-secure-password');\n</code></pre>"},{"location":"how-to/password-login/#logging-in-with-a-password","title":"Logging in with a password","text":"<p>Once registered, a user can recover their credentials and log in using only their password and username:</p> PythonTypeScript <pre><code>from reeeductio.opaque import login_with_opaque\n\nspace = login_with_opaque(\n    base_url='http://localhost:8000',\n    space_id='S...',\n    username='alice',\n    password='my-secure-password',\n)\n# space is now authenticated and ready to use\n</code></pre> <pre><code>import { loginWithOpaque } from 'reeeductio';\n\nconst space = await loginWithOpaque({\n  baseUrl: 'http://localhost:8000',\n  spaceId: 'S...',\n  username: 'alice',\n  password: 'my-secure-password',\n});\n// space is now authenticated and ready to use\n</code></pre>"},{"location":"how-to/password-login/#security-properties","title":"Security properties","text":"Property Guarantee Password never leaves the client \u2705 The OPAQUE protocol is designed so the server cannot learn your password Server compromise \u2705 An attacker who steals the server database cannot recover passwords offline (no password hash to crack) Credential recovery \u2705 You can recover <code>privateKey</code> and <code>symmetricRoot</code> from your password alone Rate limiting \u2705 The server enforces rate limits on OPAQUE login attempts Password change \u2705 Re-registers with new password; public key and space ID unchanged <p>Not a backup for lost credentials</p> <p>OPAQUE lets you recover credentials if you registered a password before losing them. If you never registered a password and lose your private key, the data is unrecoverable \u2014 by design.</p>"},{"location":"how-to/password-login/#related","title":"Related","text":"<ul> <li>Spaces \u2014 how authentication normally works (challenge-response)</li> <li>Tool Accounts \u2014 for bot/service accounts that don't need password login</li> </ul>"},{"location":"how-to/self-hosting/","title":"Self-Hosting","text":"<p>This guide covers deploying rEEEductio for production use, beyond the local development setup in Running the Server.</p>"},{"location":"how-to/self-hosting/#what-you-need","title":"What you need","text":"<ul> <li>A Linux server or container platform (Docker, Kubernetes, Cloud Run, etc.)</li> <li>An S3-compatible object store for blob storage (AWS S3, MinIO, GCS, etc.)</li> <li>A PostgreSQL or SQLite database (SQLite is fine for small deployments)</li> <li>A domain name and TLS certificate (Let's Encrypt works well)</li> </ul>"},{"location":"how-to/self-hosting/#configuration-overview","title":"Configuration overview","text":"<p>The server is configured with a YAML file. Key differences from the dev config:</p> Setting Dev Production <code>auto_create_spaces</code> <code>true</code> <code>false</code> <code>blob_store.type</code> <code>minio</code> <code>s3</code> (or minio with persistent storage) <code>environment</code> <code>development</code> <code>production</code> <code>debug</code> <code>true</code> <code>false</code> <code>logging.format</code> <code>text</code> <code>json</code> <code>server.jwt_secret</code> random Fixed, from a secrets manager"},{"location":"how-to/self-hosting/#production-config-file","title":"Production config file","text":"<pre><code>server:\n  host: 0.0.0.0\n  port: 8000\n  jwt_secret: &lt;generate with: python -c \"import secrets; print(secrets.token_hex(32))\"&gt;\n  jwt_algorithm: HS256\n  jwt_expiry_hours: 24\n  challenge_expiry_seconds: 300\n\ndatabase:\n  type: sqlite\n\nblob_store:\n  type: s3\n  bucket_name: my-reeeductio-blobs\n  region_name: us-east-1\n  # Use IAM roles on AWS instead of hardcoding keys:\n  # access_key_id: ...\n  # secret_access_key: ...\n  presigned_url_expiration: 3600\n\nadmin:\n  auto_create_spaces: false   # require admin registration in production\n\nenvironment: production\ndebug: false\n\nlogging:\n  level: INFO\n  format: json\n  file: /var/log/reeeductio/app.log\n  max_bytes: 52428800   # 50 MB\n  backup_count: 10\n  enable_access_log: true\n</code></pre>"},{"location":"how-to/self-hosting/#docker-compose-production","title":"Docker Compose (production)","text":"<pre><code>version: '3.8'\n\nservices:\n  backend:\n    image: ghcr.io/cvwright/reeeductio:latest\n    restart: unless-stopped\n    ports:\n      - \"127.0.0.1:8000:8000\"   # bind to localhost only; put Nginx/Caddy in front\n    volumes:\n      - ./data:/data\n      - ./config/config.yaml:/config/config.yaml:ro\n      - ./logs:/var/log/reeeductio\n    environment:\n      CONFIG_FILE: /config/config.yaml\n    healthcheck:\n      test: [\"CMD\", \"curl\", \"-f\", \"http://localhost:8000/health\"]\n      interval: 30s\n      timeout: 10s\n      retries: 3\n\n  # Optional: Nginx reverse proxy for TLS termination\n  nginx:\n    image: nginx:alpine\n    restart: unless-stopped\n    ports:\n      - \"80:80\"\n      - \"443:443\"\n    volumes:\n      - ./nginx.conf:/etc/nginx/nginx.conf:ro\n      - /etc/letsencrypt:/etc/letsencrypt:ro\n    depends_on:\n      - backend\n</code></pre>"},{"location":"how-to/self-hosting/#tls-with-caddy-simpler-alternative-to-nginx","title":"TLS with Caddy (simpler alternative to Nginx)","text":"<p>Caddy handles HTTPS automatically with Let's Encrypt:</p> <pre><code>api.yourserver.com {\n    reverse_proxy localhost:8000\n}\n</code></pre> <p>Start Caddy and it provisions and renews certificates automatically.</p>"},{"location":"how-to/self-hosting/#setting-up-the-admin-space","title":"Setting up the admin space","text":"<p>With <code>auto_create_spaces: false</code>, you control which spaces exist. The admin space is the special space that tracks registered spaces and server-level users.</p> <p>Step 1. Generate admin credentials (do this once, store the keys securely)</p> <pre><code>reeeductio-admin space generate\n\n# Output:\n# Space ID:        S...\n# User ID:         U...\n# Private Key:     &lt;hex&gt;\n# Symmetric Root:  &lt;hex&gt;\n</code></pre> <p>Step 2. Copy the Space ID into your config file:</p> <pre><code>admin:\n  admin_space_id: S...   # paste the Space ID from step 1\n  auto_create_spaces: false\n</code></pre> <p>The server initializes the admin space automatically on first startup when it finds <code>admin_space_id</code> in the config. The admin credentials (private key and symmetric root) stay on your client machine \u2014 the server only needs the space ID.</p>"},{"location":"how-to/self-hosting/#blob-storage-options","title":"Blob storage options","text":""},{"location":"how-to/self-hosting/#aws-s3","title":"AWS S3","text":"<pre><code>blob_store:\n  type: s3\n  bucket_name: my-reeeductio-blobs\n  region_name: us-east-1\n  # On EC2/ECS with IAM role attached, no keys needed:\n  # access_key_id: ...\n  # secret_access_key: ...\n</code></pre> <p>Create an S3 bucket with private ACLs. Attach an IAM role to your instance/task with <code>s3:GetObject</code>, <code>s3:PutObject</code>, <code>s3:DeleteObject</code> on the bucket.</p>"},{"location":"how-to/self-hosting/#minio-self-hosted-s3","title":"MinIO (self-hosted S3)","text":"<pre><code>blob_store:\n  type: s3\n  bucket_name: reeeductio\n  endpoint_url: http://minio:9000\n  access_key_id: minioadmin\n  secret_access_key: minioadmin\n  region_name: us-east-1\n</code></pre>"},{"location":"how-to/self-hosting/#filesystem-small-deployments-only","title":"Filesystem (small deployments only)","text":"<pre><code>blob_store:\n  type: filesystem\n  path: /data/blobs\n</code></pre> <p>Ensure <code>/data/blobs</code> is on a persistent volume with adequate capacity.</p>"},{"location":"how-to/self-hosting/#backup-and-restore","title":"Backup and restore","text":""},{"location":"how-to/self-hosting/#backup","title":"Backup","text":"<pre><code># Stop the server first for a consistent snapshot\ndocker compose stop backend\n\n# Backup SQLite databases and any filesystem blobs\ntar -czf \"backup-$(date +%Y%m%d-%H%M%S).tar.gz\" data/\n\ndocker compose start backend\n</code></pre> <p>For S3 blobs, use S3 versioning or cross-region replication for durability \u2014 no separate backup needed.</p>"},{"location":"how-to/self-hosting/#restore","title":"Restore","text":"<pre><code>docker compose down\ntar -xzf backup-20260101-120000.tar.gz\ndocker compose up -d\n</code></pre>"},{"location":"how-to/self-hosting/#monitoring","title":"Monitoring","text":"<p>The <code>/health</code> endpoint returns <code>{\"status\": \"healthy\"}</code> when the server is up. Use it for uptime monitoring and load balancer health checks.</p> <pre><code>curl https://api.yourserver.com/health\n</code></pre> <p>For structured logs, set <code>logging.format: json</code> and ship logs to your preferred aggregation service (CloudWatch, Datadog, Loki, etc.).</p>"},{"location":"how-to/self-hosting/#keeping-up-to-date","title":"Keeping up to date","text":"<pre><code># Pull latest image\ndocker compose pull\n\n# Restart with zero-downtime (if behind a load balancer)\ndocker compose up -d --no-deps backend\n</code></pre> <p>Check the GitHub releases for changelogs before upgrading.</p>"},{"location":"how-to/self-hosting/#related","title":"Related","text":"<ul> <li>Running the Server \u2014 local development setup</li> <li>Server Configuration \u2014 full config reference</li> </ul>"},{"location":"how-to/send-messages/","title":"Send Messages","text":"<p>This guide covers sending and receiving encrypted messages, working with multiple topics, and subscribing to real-time updates.</p>"},{"location":"how-to/send-messages/#basic-send-and-receive","title":"Basic send and receive","text":"<p>Messages are encrypted client-side before sending. The server stores only ciphertext.</p> PythonTypeScript <pre><code>from reeeductio import Space\n\nspace = Space(\n    space_id='S...',\n    member_id='U...',\n    private_key=...,\n    symmetric_root=...,\n    base_url='http://localhost:8000',\n)\n\n# Send\nresult = space.post_encrypted_message(\n    topic_id='general',\n    msg_type='chat.text',\n    data=b'Hello, encrypted world!',\n)\nprint('Posted:', result.message_hash)\n\n# Receive\nmsgs = space.get_messages('general')\nfor msg in msgs:\n    text = space.decrypt_message_data(msg, 'general')\n    print(f'[{msg.message_hash[:12]}] {text.decode()}')\n</code></pre> <pre><code>import { Space, stringToBytes, bytesToString } from 'reeeductio';\n\nconst space = new Space({ spaceId, keyPair, symmetricRoot, baseUrl: 'http://localhost:8000' });\n\n// Send\nconst result = await space.postEncryptedMessage('general', 'chat.text', stringToBytes('Hello!'));\nconsole.log('Posted:', result.message_hash);\n\n// Receive\nconst { messages } = await space.getMessages('general');\nfor (const msg of messages) {\n  console.log(`[${msg.message_hash.slice(0, 12)}] ${bytesToString(space.decryptMessageData(msg, 'general'))}`);\n}\n</code></pre>"},{"location":"how-to/send-messages/#message-types","title":"Message types","text":"<p>The <code>msg_type</code> / <code>type</code> field is a free-form string you define. Use it to distinguish different kinds of events in a topic:</p> Type Meaning <code>chat.text</code> Plain text message <code>chat.image</code> Image (data is a blob ID) <code>event.join</code> User joined the space <code>event.leave</code> User left <code>file.upload</code> File uploaded (data is a blob ID) PythonTypeScript <pre><code>import json\n\n# Post a structured JSON message\npayload = json.dumps({'text': 'Hello!', 'mentions': ['U...']}).encode()\nspace.post_encrypted_message('general', 'chat.text', payload)\n</code></pre> <pre><code>const payload = JSON.stringify({ text: 'Hello!', mentions: ['U...'] });\nawait space.postEncryptedMessage('general', 'chat.text', stringToBytes(payload));\n</code></pre>"},{"location":"how-to/send-messages/#working-with-multiple-topics","title":"Working with multiple topics","text":"<p>Topics are independent \u2014 create as many as you like. Topic IDs must be lowercase alphanumeric + hyphens or underscores, 2\u201364 characters.</p> PythonTypeScript <pre><code>space.post_encrypted_message('general', 'chat.text', b'Hi everyone')\nspace.post_encrypted_message('engineering', 'chat.text', b'Deploy complete')\nspace.post_encrypted_message('announcements', 'event.announcement', b'New release!')\n</code></pre> <pre><code>await space.postEncryptedMessage('general', 'chat.text', stringToBytes('Hi everyone'));\nawait space.postEncryptedMessage('engineering', 'chat.text', stringToBytes('Deploy complete'));\nawait space.postEncryptedMessage('announcements', 'event.announcement', stringToBytes('New release!'));\n</code></pre>"},{"location":"how-to/send-messages/#querying-messages","title":"Querying messages","text":"<p>Filter messages by time range or limit the count:</p> PythonTypeScript <pre><code>import time\n\n# Last hour\none_hour_ago = int(time.time() * 1000) - 3_600_000\nrecent = space.get_messages('general', from_timestamp=one_hour_ago)\n\n# Most recent 10 messages (reverse-chronological)\nlatest = space.get_messages(\n    'general',\n    from_timestamp=9999999999999,\n    to_timestamp=0,\n    limit=10,\n)\n</code></pre> <pre><code>const oneHourAgo = Date.now() - 3_600_000;\nconst { messages: recent } = await space.getMessages('general', { from: oneHourAgo });\n\n// Most recent 10\nconst { messages: latest } = await space.getMessages('general', {\n  from: Date.now(),\n  to: 0,\n  limit: 10,\n});\n</code></pre>"},{"location":"how-to/send-messages/#verifying-the-message-chain","title":"Verifying the message chain","text":"<p>By default, the SDK verifies that <code>prev_hash</code> links are intact on every fetch. If the chain is broken, a <code>ChainError</code> is raised. You can also validate explicitly:</p> PythonTypeScript <pre><code>from reeeductio.messages import validate_message_chain\n\nmsgs = space.get_messages('general')\nvalid = validate_message_chain(space.space_id, msgs)\nprint('Chain valid:', valid)\n</code></pre> <pre><code>import { validateMessageChainWithAnchor } from 'reeeductio';\n\nconst { messages } = await space.getMessages('general');\nconst valid = validateMessageChainWithAnchor(space.spaceId, messages, null);\nconsole.log('Chain valid:', valid);\n</code></pre>"},{"location":"how-to/send-messages/#real-time-updates-websocket","title":"Real-time updates (WebSocket)","text":"<p>Subscribe to a topic to receive new messages as they arrive:</p> PythonTypeScript <pre><code>import asyncio\nfrom reeeductio import Space\n\nasync def listen():\n    space = Space(...)\n    async with space.subscribe('general') as stream:\n        async for msg in stream:\n            text = space.decrypt_message_data(msg, 'general')\n            print(f'New message: {text.decode()}')\n\nasyncio.run(listen())\n</code></pre> <pre><code>// WebSocket support coming soon \u2014 use polling in the meantime:\nsetInterval(async () =&gt; {\n  const { messages } = await space.getMessages('general', { from: lastSeen });\n  for (const msg of messages) {\n    console.log(bytesToString(space.decryptMessageData(msg, 'general')));\n    lastSeen = msg.server_timestamp;\n  }\n}, 1000);\n</code></pre>"},{"location":"how-to/send-messages/#attaching-files","title":"Attaching files","text":"<p>For large payloads, upload a blob and include its ID in the message:</p> PythonTypeScript <pre><code># Upload the file\nwith open('photo.jpg', 'rb') as f:\n    result = space.encrypt_and_upload_blob(f.read())\n\n# Post a reference message\nimport json\npayload = json.dumps({'blob_id': result.blob_id, 'filename': 'photo.jpg'}).encode()\nspace.post_encrypted_message('photos', 'chat.image', payload)\n</code></pre> <pre><code>const data = new Uint8Array(await file.arrayBuffer());\nconst { blob_id, key } = await space.encryptAndUploadBlob(data);\n\nconst payload = JSON.stringify({ blob_id, filename: file.name });\nawait space.postEncryptedMessage('photos', 'chat.image', stringToBytes(payload));\n</code></pre>"},{"location":"how-to/send-messages/#related","title":"Related","text":"<ul> <li>Store Files \u2014 uploading and downloading blob attachments</li> <li>Topics &amp; Messages \u2014 concept overview</li> <li>Access Control \u2014 controlling who can post</li> </ul>"},{"location":"how-to/store-files/","title":"Store Files","text":"<p>This guide covers uploading, downloading, and deleting encrypted files (blobs).</p>"},{"location":"how-to/store-files/#how-blob-encryption-works","title":"How blob encryption works","text":"<p>Every blob is encrypted with a randomly-generated data encryption key (DEK). The DEK is returned to you when you upload and must be stored alongside the blob ID \u2014 the server never has it.</p> <p>This is different from message and state encryption, where keys are derived from <code>symmetric_root</code>. Blob DEKs are ephemeral per-blob keys; you are responsible for storing them.</p>"},{"location":"how-to/store-files/#uploading-a-file","title":"Uploading a file","text":"PythonTypeScript <pre><code>from reeeductio import Space\n\nspace = Space(\n    space_id='S...',\n    member_id='U...',\n    private_key=...,\n    symmetric_root=...,\n    base_url='http://localhost:8000',\n)\n\nwith open('report.pdf', 'rb') as f:\n    data = f.read()\n\nresult = space.encrypt_and_upload_blob(data)\nprint('Blob ID:', result.blob_id)   # B...\nprint('DEK (hex):', result.dek.hex())  # save this!\n</code></pre> <pre><code>import { Space } from 'reeeductio';\n\nconst space = new Space({ spaceId, keyPair, symmetricRoot, baseUrl: 'http://localhost:8000' });\n\nconst data = new Uint8Array(await file.arrayBuffer());\nconst result = await space.encryptAndUploadBlob(data);\n\nconsole.log('Blob ID:', result.blob_id);   // B...\nconsole.log('DEK (hex):', Buffer.from(result.key).toString('hex'));  // save this!\n</code></pre> <p>Save your DEK</p> <p>Without the DEK you cannot decrypt the blob. Store it somewhere safe alongside the blob ID \u2014 for example, encrypted inside a message or in the space's State.</p>"},{"location":"how-to/store-files/#downloading-and-decrypting","title":"Downloading and decrypting","text":"PythonTypeScript <pre><code># Use the blob_id and DEK returned at upload time\ndata = space.download_and_decrypt_blob(\n    blob_id='B...',\n    key=dek,   # the 32-byte DEK returned at upload\n)\n\nwith open('report-copy.pdf', 'wb') as f:\n    f.write(data)\n</code></pre> <pre><code>const data = await space.downloadAndDecryptBlob(\n  'B...',\n  key,   // the Uint8Array DEK returned at upload\n);\n// data is a Uint8Array of the original file bytes\n</code></pre>"},{"location":"how-to/store-files/#storing-the-dek-alongside-the-blob-id","title":"Storing the DEK alongside the blob ID","text":"<p>The most common pattern is to post both the blob ID and the DEK as an encrypted message, so other authorized members can retrieve it:</p> PythonTypeScript <pre><code>import json\n\nresult = space.encrypt_and_upload_blob(data)\n\n# Store blob ID + DEK as an encrypted message\npayload = json.dumps({\n    'blob_id': result.blob_id,\n    'dek': result.dek.hex(),\n    'filename': 'report.pdf',\n    'content_type': 'application/pdf',\n}).encode()\nspace.post_encrypted_message('files', 'file.upload', payload)\n</code></pre> <pre><code>const result = await space.encryptAndUploadBlob(data);\n\nconst payload = JSON.stringify({\n  blob_id: result.blob_id,\n  dek: Buffer.from(result.key).toString('hex'),\n  filename: 'report.pdf',\n  content_type: 'application/pdf',\n});\nawait space.postEncryptedMessage('files', 'file.upload', stringToBytes(payload));\n</code></pre> <p>Recipients read the message, extract the blob ID and DEK, then download and decrypt:</p> PythonTypeScript <pre><code>msgs = space.get_messages('files')\nfor msg in msgs:\n    meta = json.loads(space.decrypt_message_data(msg, 'files'))\n    blob = space.download_and_decrypt_blob(\n        blob_id=meta['blob_id'],\n        key=bytes.fromhex(meta['dek']),\n    )\n    with open(meta['filename'], 'wb') as f:\n        f.write(blob)\n</code></pre> <pre><code>const { messages } = await space.getMessages('files');\nfor (const msg of messages) {\n  const meta = JSON.parse(bytesToString(space.decryptMessageData(msg, 'files')));\n  const blob = await space.downloadAndDecryptBlob(\n    meta.blob_id,\n    Uint8Array.from(Buffer.from(meta.dek, 'hex')),\n  );\n  // blob is a Uint8Array of the file bytes\n}\n</code></pre>"},{"location":"how-to/store-files/#deleting-a-blob","title":"Deleting a blob","text":"PythonTypeScript <pre><code>space.delete_blob('B...')\n</code></pre> <pre><code>await space.deleteBlob('B...');\n</code></pre> <p>Deletion is permanent</p> <p>Once a blob is deleted, it cannot be recovered. Any messages containing the blob ID will have a broken reference.</p>"},{"location":"how-to/store-files/#uploading-without-encryption","title":"Uploading without encryption","text":"<p>If you need to store files that don't need to be encrypted (e.g. publicly readable assets), use the plaintext upload:</p> PythonTypeScript <pre><code>result = space.upload_plaintext_blob(data)\nprint('Blob ID:', result.blob_id)\n\n# Download (no key needed)\ndata = space.download_plaintext_blob('B...')\n</code></pre> <pre><code>import { uploadBlob, downloadBlob } from 'reeeductio';\n\nconst token = await space.authenticate();\nconst result = await uploadBlob(fetch, space.baseUrl, token, space.spaceId, data);\nconst downloaded = await downloadBlob(fetch, space.baseUrl, token, space.spaceId, result.blob_id);\n</code></pre>"},{"location":"how-to/store-files/#related","title":"Related","text":"<ul> <li>Blobs \u2014 concept overview</li> <li>Send Messages \u2014 posting blob IDs as messages</li> <li>Topics &amp; Messages</li> </ul>"},{"location":"how-to/tool-accounts/","title":"Tool Accounts","text":"<p>Tools are bot or service accounts that authenticate with the server just like users, but without the ability to have roles \u2014 every permission must be explicitly granted. Use them for:</p> <ul> <li>Automated posting bots (alerts, notifications, CI/CD updates)</li> <li>Background workers that read or process messages</li> <li>Third-party integrations</li> <li>Webhook receivers</li> </ul>"},{"location":"how-to/tool-accounts/#creating-a-tool-account","title":"Creating a tool account","text":""},{"location":"how-to/tool-accounts/#step-1-generate-a-keypair-for-the-tool","title":"Step 1 \u2014 Generate a keypair for the tool","text":"<p>Each tool has its own Ed25519 keypair. Generate one and save both keys:</p> PythonTypeScript <pre><code>import os\nfrom reeeductio.crypto import generate_keypair, to_tool_id\n\nprivate_key, public_key = generate_keypair()\ntool_id = to_tool_id(public_key)   # starts with 'T'\n\nprint('Tool ID:      ', tool_id)\nprint('Private key:  ', private_key.hex())\nprint('Public key:   ', public_key.hex())\n# Store both keys \u2014 you need the public key to reconstruct the tool ID later\n</code></pre> <pre><code>import { generateKeyPair, toToolId } from 'reeeductio';\n\nconst keyPair = await generateKeyPair();\nconst toolId = toToolId(keyPair.publicKey);  // starts with 'T'\n\nconsole.log('Tool ID:     ', toolId);\nconsole.log('Private key: ', Buffer.from(keyPair.privateKey).toString('hex'));\nconsole.log('Public key:  ', Buffer.from(keyPair.publicKey).toString('hex'));\n</code></pre>"},{"location":"how-to/tool-accounts/#step-2-register-the-tool-in-the-space-as-admin","title":"Step 2 \u2014 Register the tool in the space (as admin)","text":"PythonCLI <pre><code>from reeeductio import Space\n\nspace = Space(\n    space_id='S...',\n    member_id='U...',   # your user ID (admin)\n    private_key=...,\n    symmetric_root=...,\n    base_url='http://localhost:8000',\n)\n\nspace.create_tool('T...')   # the tool's T-prefixed ID\n</code></pre> <pre><code>reeeductio-admin tool add T... \\\n    --space-key &lt;your-private-key-hex&gt; \\\n    --symmetric-root &lt;symmetric-root-hex&gt;\n</code></pre>"},{"location":"how-to/tool-accounts/#step-3-grant-capabilities-to-the-tool","title":"Step 3 \u2014 Grant capabilities to the tool","text":"<p>Tools have no permissions by default. Grant exactly what they need:</p> PythonCLI <pre><code># Allow the tool to post to the alerts topic\nspace.grant_capability_to_tool(\n    tool_id='T...',\n    cap_id='post-alerts',\n    capability={'op': 'create', 'path': 'topics/alerts/messages/{any}'},\n)\n\n# Allow the tool to read all topics (e.g. for a read-only bot)\nspace.grant_capability_to_tool(\n    tool_id='T...',\n    cap_id='read-all',\n    capability={'op': 'read', 'path': 'topics/{any}'},\n)\n</code></pre> <pre><code>reeeductio-admin tool grant T... \\\n    --cap-id post-alerts \\\n    --op create \\\n    --path \"topics/alerts/messages/{any}\"\n</code></pre>"},{"location":"how-to/tool-accounts/#using-a-tool-account-in-code","title":"Using a tool account in code","text":"<p>Tools authenticate and make API calls exactly like users. Pass the tool's private key, its public key, and the <code>symmetric_root</code> to your bot process:</p> PythonTypeScript <pre><code>import os\nfrom reeeductio import Space\nfrom reeeductio.crypto import to_tool_id\n\n# Load credentials from environment\ntool_private_key  = bytes.fromhex(os.environ['TOOL_PRIVATE_KEY'])\ntool_public_key   = bytes.fromhex(os.environ['TOOL_PUBLIC_KEY'])\nsymmetric_root    = bytes.fromhex(os.environ['SYMMETRIC_ROOT'])\nspace_id          = os.environ['SPACE_ID']\n\ntool_id = to_tool_id(tool_public_key)\n\nspace = Space(\n    space_id=space_id,\n    member_id=tool_id,\n    private_key=tool_private_key,\n    symmetric_root=symmetric_root,\n    base_url='https://your-server.example.com',\n)\n\n# Post an alert\nspace.post_encrypted_message('alerts', 'alert.info', b'Build succeeded!')\n</code></pre> <pre><code>import { Space, toToolId, stringToBytes } from 'reeeductio';\n\nconst privateKey    = Uint8Array.from(Buffer.from(process.env.TOOL_PRIVATE_KEY!, 'hex'));\nconst publicKey     = Uint8Array.from(Buffer.from(process.env.TOOL_PUBLIC_KEY!, 'hex'));\nconst symmetricRoot = Uint8Array.from(Buffer.from(process.env.SYMMETRIC_ROOT!, 'hex'));\nconst spaceId       = process.env.SPACE_ID!;\n\nconst keyPair = { privateKey, publicKey };\nconst toolId  = toToolId(publicKey);\n\nconst space = new Space({ spaceId, keyPair, symmetricRoot, baseUrl: 'https://your-server.example.com' });\n\nawait space.postEncryptedMessage('alerts', 'alert.info', stringToBytes('Build succeeded!'));\n</code></pre>"},{"location":"how-to/tool-accounts/#storing-tool-credentials-safely","title":"Storing tool credentials safely","text":"<p>Never hardcode credentials. Use your platform's secrets manager:</p> Platform Recommended approach Docker / Compose <code>secrets:</code> block or <code>.env</code> file Kubernetes <code>Secret</code> objects, mounted as env vars GitHub Actions Encrypted Actions secrets AWS Secrets Manager or Parameter Store Cloud Run Secret Manager <p>The bot needs four values: <code>TOOL_PRIVATE_KEY</code>, <code>TOOL_PUBLIC_KEY</code>, <code>SYMMETRIC_ROOT</code>, <code>SPACE_ID</code>.</p>"},{"location":"how-to/tool-accounts/#removing-a-tool","title":"Removing a tool","text":"PythonCLI <pre><code>space.set_plaintext_state(f'auth/tools/{tool_id}', '')\n</code></pre> <pre><code>reeeductio-admin tool remove T...\n</code></pre> <p>After removal, the tool's authentication attempts will be rejected by the server.</p>"},{"location":"how-to/tool-accounts/#example-cicd-notification-bot-github-actions","title":"Example: CI/CD notification bot (GitHub Actions)","text":"<pre><code>- name: Notify rEEEductio\n  env:\n    TOOL_PRIVATE_KEY: ${{ secrets.REEEDUCTIO_TOOL_PRIVATE_KEY }}\n    TOOL_PUBLIC_KEY:  ${{ secrets.REEEDUCTIO_TOOL_PUBLIC_KEY }}\n    SYMMETRIC_ROOT:   ${{ secrets.REEEDUCTIO_SYMMETRIC_ROOT }}\n    SPACE_ID:         ${{ secrets.REEEDUCTIO_SPACE_ID }}\n  run: |\n    pip install reeeductio\n    python - &lt;&lt;'EOF'\n    import os\n    from reeeductio import Space\n    from reeeductio.crypto import to_tool_id\n\n    tool_id = to_tool_id(bytes.fromhex(os.environ['TOOL_PUBLIC_KEY']))\n    space = Space(\n        space_id=os.environ['SPACE_ID'],\n        member_id=tool_id,\n        private_key=bytes.fromhex(os.environ['TOOL_PRIVATE_KEY']),\n        symmetric_root=bytes.fromhex(os.environ['SYMMETRIC_ROOT']),\n        base_url='https://your-server.example.com',\n    )\n    space.post_encrypted_message('alerts', 'ci.deploy', b'Deployed to production!')\n    EOF\n</code></pre>"},{"location":"how-to/tool-accounts/#related","title":"Related","text":"<ul> <li>Add Users \u2014 adding human members</li> <li>Manage Permissions \u2014 roles and capabilities in depth</li> <li>Access Control \u2014 concept overview</li> </ul>"},{"location":"reference/api/","title":"REST API Reference","text":"<p>The rEEEductio server exposes a JSON REST API. All space-scoped endpoints are under <code>/spaces/{space_id}/</code>.</p> <p>Most endpoints require a Bearer token obtained via the authentication flow below.</p>"},{"location":"reference/api/#authentication","title":"Authentication","text":""},{"location":"reference/api/#request-a-challenge","title":"Request a challenge","text":"<pre><code>POST /spaces/{space_id}/auth/challenge\n</code></pre> <p>No authentication required.</p> <p>Request body:</p> <pre><code>{ \"public_key\": \"U...\" }\n</code></pre> <p>Response:</p> <pre><code>{\n  \"challenge\": \"&lt;base64-encoded nonce&gt;\",\n  \"expires_at\": 1700000000\n}\n</code></pre>"},{"location":"reference/api/#verify-and-obtain-a-token","title":"Verify and obtain a token","text":"<pre><code>POST /spaces/{space_id}/auth/verify\n</code></pre> <p>Request body:</p> <pre><code>{\n  \"public_key\": \"U...\",\n  \"challenge\": \"&lt;challenge from previous step&gt;\",\n  \"signature\": \"&lt;base64-encoded Ed25519 signature over the challenge&gt;\"\n}\n</code></pre> <p>Response:</p> <pre><code>{\n  \"token\": \"&lt;JWT&gt;\",\n  \"expires_at\": 1700086400\n}\n</code></pre> <p>Use the token in subsequent requests:</p> <pre><code>Authorization: Bearer &lt;token&gt;\n</code></pre>"},{"location":"reference/api/#refresh-a-token","title":"Refresh a token","text":"<pre><code>POST /spaces/{space_id}/auth/refresh\n</code></pre> <p>Auth required: Yes</p> <p>Returns a new <code>TokenResponse</code> with an extended expiry.</p>"},{"location":"reference/api/#messages","title":"Messages","text":""},{"location":"reference/api/#get-messages","title":"Get messages","text":"<pre><code>GET /spaces/{space_id}/topics/{topic_id}/messages\n</code></pre> <p>Auth required: Yes</p> <p>Query parameters:</p> Parameter Type Description <code>from</code> integer Start timestamp (ms, inclusive) <code>to</code> integer End timestamp (ms, inclusive) <code>limit</code> integer Max results (1\u20131000, default 100) <p>Response:</p> <pre><code>{\n  \"messages\": [\n    {\n      \"message_hash\": \"M...\",\n      \"topic_id\": \"general\",\n      \"type\": \"chat.text\",\n      \"prev_hash\": \"M...\",\n      \"data\": \"&lt;base64&gt;\",\n      \"sender\": \"U...\",\n      \"signature\": \"&lt;base64&gt;\",\n      \"server_timestamp\": 1700000000000\n    }\n  ],\n  \"has_more\": false\n}\n</code></pre>"},{"location":"reference/api/#post-a-message","title":"Post a message","text":"<pre><code>POST /spaces/{space_id}/topics/{topic_id}/messages\n</code></pre> <p>Auth required: Yes</p> <p>Topic ID format: 2\u201364 characters, lowercase alphanumeric with hyphens and underscores (<code>[a-z0-9][a-z0-9_-]*[a-z0-9]</code>).</p> <p>Request body:</p> <pre><code>{\n  \"type\": \"chat.text\",\n  \"prev_hash\": \"M...\",\n  \"data\": \"&lt;base64-encoded payload, max 100 KB&gt;\",\n  \"message_hash\": \"&lt;SHA-256 of canonical fields&gt;\",\n  \"signature\": \"&lt;Ed25519 signature over message_hash&gt;\"\n}\n</code></pre> <p>Response (201):</p> <pre><code>{\n  \"message_hash\": \"M...\",\n  \"server_timestamp\": 1700000000000\n}\n</code></pre> <p>Error 409 Conflict: <code>prev_hash</code> does not match the current chain head. Fetch the latest message and retry.</p>"},{"location":"reference/api/#get-a-single-message","title":"Get a single message","text":"<pre><code>GET /spaces/{space_id}/topics/{topic_id}/messages/{message_hash}\n</code></pre> <p>Auth required: Yes</p> <p>Returns a single <code>Message</code> object or 404 if not found.</p>"},{"location":"reference/api/#state","title":"State","text":"<p>State is an event-sourced key-value store backed by the reserved <code>state</code> topic. Every write is a message; the current value at a path is the data from the most recent message for that path.</p>"},{"location":"reference/api/#get-state-history","title":"Get state history","text":"<pre><code>GET /spaces/{space_id}/state\n</code></pre> <p>Auth required: Yes</p> <p>Accepts the same <code>from</code>, <code>to</code>, <code>limit</code> query parameters as the messages endpoint.</p> <p>Returns a <code>MessagesResponse</code> of all state-change messages.</p>"},{"location":"reference/api/#get-value-at-a-path","title":"Get value at a path","text":"<pre><code>GET /spaces/{space_id}/state/{path}\n</code></pre> <p>Auth required: Yes</p> <p>Returns the most recent <code>Message</code> whose <code>type</code> equals <code>path</code>, or 404 if no value has been written to that path.</p>"},{"location":"reference/api/#set-a-value","title":"Set a value","text":"<pre><code>PUT /spaces/{space_id}/state/{path}\n</code></pre> <p>Auth required: Yes</p> <p>Request body: Same shape as posting a message. The <code>type</code> field must equal the <code>path</code>.</p> <p>Response:</p> <pre><code>{\n  \"message_hash\": \"M...\",\n  \"server_timestamp\": 1700000000000\n}\n</code></pre>"},{"location":"reference/api/#blobs","title":"Blobs","text":"<p>Blob IDs are content-addressed: <code>B</code> + base64url(SHA-256 of the encrypted content).</p>"},{"location":"reference/api/#upload-a-blob","title":"Upload a blob","text":"<pre><code>PUT /spaces/{space_id}/blobs/{blob_id}\n</code></pre> <p>Auth required: Yes</p> <p>Request body: Raw binary data (<code>application/octet-stream</code>).</p> <p>Response (201):</p> <pre><code>{ \"blob_id\": \"B...\", \"size\": 102400 }\n</code></pre> <p>For S3-backed deployments the response may instead be:</p> <pre><code>{ \"blob_id\": \"B...\", \"upload_url\": \"https://...\" }\n</code></pre> <p>In that case, upload the raw bytes directly to <code>upload_url</code> via HTTP <code>PUT</code>.</p>"},{"location":"reference/api/#download-a-blob","title":"Download a blob","text":"<pre><code>GET /spaces/{space_id}/blobs/{blob_id}\n</code></pre> <p>Auth required: Yes</p> <p>Returns raw binary data, or a redirect/presigned URL for S3-backed deployments.</p>"},{"location":"reference/api/#delete-a-blob","title":"Delete a blob","text":"<pre><code>DELETE /spaces/{space_id}/blobs/{blob_id}\n</code></pre> <p>Auth required: Yes</p> <p>Returns 204 No Content.</p>"},{"location":"reference/api/#data-kv-store","title":"Data (KV store)","text":"<p>The data store is a simple signed key-value store. Unlike state, it has no hash chain; each entry is independently signed.</p>"},{"location":"reference/api/#get-a-value","title":"Get a value","text":"<pre><code>GET /spaces/{space_id}/data/{path}\n</code></pre> <p>Auth required: Yes</p> <p>Response:</p> <pre><code>{\n  \"data\": \"&lt;base64&gt;\",\n  \"signature\": \"&lt;base64 Ed25519 signature&gt;\",\n  \"signed_by\": \"U...\",\n  \"signed_at\": 1700000000000\n}\n</code></pre>"},{"location":"reference/api/#set-a-value_1","title":"Set a value","text":"<pre><code>PUT /spaces/{space_id}/data/{path}\n</code></pre> <p>Auth required: Yes</p> <p>Request body:</p> <pre><code>{\n  \"data\": \"&lt;base64&gt;\",\n  \"signature\": \"&lt;Ed25519 signature over space_id|path|data|signed_at&gt;\",\n  \"signed_by\": \"U...\",\n  \"signed_at\": 1700000000000\n}\n</code></pre> <p>Response:</p> <pre><code>{ \"path\": \"...\", \"signed_at\": 1700000000000 }\n</code></pre>"},{"location":"reference/api/#delete-a-value","title":"Delete a value","text":"<pre><code>DELETE /spaces/{space_id}/data/{path}\n</code></pre> <p>Auth required: Yes</p> <p>Returns 204 No Content.</p>"},{"location":"reference/api/#websocket","title":"WebSocket","text":""},{"location":"reference/api/#connect-to-the-real-time-stream","title":"Connect to the real-time stream","text":"<pre><code>WebSocket /spaces/{space_id}/stream?token=&lt;JWT&gt;\n</code></pre> <p>The JWT token is passed as a query parameter (not a header) because browser WebSocket APIs do not support custom headers.</p> <p>Once connected, the server pushes new messages as JSON-encoded <code>Message</code> objects as they are posted to any topic in the space.</p> <pre><code>const wsUrl = `wss://my-server.example.com/spaces/${spaceId}/stream?token=${token}`;\nconst ws = new WebSocket(wsUrl);\nws.onmessage = (event) =&gt; {\n  const msg = JSON.parse(event.data);\n  // msg is a Message object\n};\n</code></pre>"},{"location":"reference/api/#health-check","title":"Health check","text":"<pre><code>GET /health\n</code></pre> <p>No authentication required.</p> <p>Response:</p> <pre><code>{ \"status\": \"healthy\", \"timestamp\": 1700000000000 }\n</code></pre>"},{"location":"reference/api/#admin-api","title":"Admin API","text":"<p>These endpoints use a separate admin authentication session. The admin space ID is configured server-side.</p>"},{"location":"reference/api/#admin-challenge","title":"Admin challenge","text":"<pre><code>POST /admin/auth/challenge\n</code></pre> <p>Same as the space challenge, but targets the admin space without needing to know its ID in advance.</p>"},{"location":"reference/api/#admin-verify","title":"Admin verify","text":"<pre><code>POST /admin/auth/verify\n</code></pre> <p>Same as space verify. Returns a JWT scoped to the admin space.</p>"},{"location":"reference/api/#get-admin-space-id","title":"Get admin space ID","text":"<pre><code>GET /admin/space\n</code></pre> <p>Auth required: Yes (admin token)</p> <pre><code>{ \"space_id\": \"S...\" }\n</code></pre>"},{"location":"reference/api/#admin-delete-blob","title":"Admin delete blob","text":"<pre><code>DELETE /admin/blobs/{blob_id}\n</code></pre> <p>Auth required: Yes (admin token)</p> <p>Deletes a blob across all spaces. Useful for cleaning up orphaned blobs.</p>"},{"location":"reference/api/#opaque-password-based-key-recovery","title":"OPAQUE (password-based key recovery)","text":"<p>These endpoints implement the OPAQUE protocol for password-based credential wrapping. They do not grant access directly \u2014 a successful OPAQUE login recovers the user's <code>privateKey</code> and <code>symmetricRoot</code>, which are then used for the standard challenge-response flow.</p>"},{"location":"reference/api/#enable-opaque-for-a-space","title":"Enable OPAQUE for a space","text":"<pre><code>POST /spaces/{space_id}/opaque/setup\n</code></pre> <p>Auth required: Yes (space admin)</p> <p>Initializes the OPAQUE server-side setup for this space.</p>"},{"location":"reference/api/#registration-step-1-of-2","title":"Registration (step 1 of 2)","text":"<pre><code>POST /spaces/{space_id}/opaque/register/init\n</code></pre> <p>Auth required: Yes (space member)</p> <pre><code>{\n  \"username\": \"alice\",\n  \"registration_request\": \"&lt;base64 OPAQUE RegistrationRequest&gt;\"\n}\n</code></pre> <p>Response:</p> <pre><code>{ \"registration_response\": \"&lt;base64 OPAQUE RegistrationResponse&gt;\" }\n</code></pre>"},{"location":"reference/api/#registration-step-2-of-2","title":"Registration (step 2 of 2)","text":"<pre><code>POST /spaces/{space_id}/opaque/register/finish\n</code></pre> <p>Auth required: Yes</p> <pre><code>{\n  \"username\": \"alice\",\n  \"registration_record\": \"&lt;base64 OPAQUE RegistrationUpload&gt;\"\n}\n</code></pre>"},{"location":"reference/api/#login-step-1-of-2","title":"Login (step 1 of 2)","text":"<pre><code>POST /spaces/{space_id}/opaque/login/init\n</code></pre> <p>No authentication required.</p> <pre><code>{\n  \"username\": \"alice\",\n  \"credential_request\": \"&lt;base64 OPAQUE CredentialRequest&gt;\"\n}\n</code></pre> <p>Response:</p> <pre><code>{ \"credential_response\": \"&lt;base64 OPAQUE CredentialResponse&gt;\" }\n</code></pre>"},{"location":"reference/api/#login-step-2-of-2","title":"Login (step 2 of 2)","text":"<pre><code>POST /spaces/{space_id}/opaque/login/finish\n</code></pre> <p>No authentication required.</p> <pre><code>{\n  \"username\": \"alice\",\n  \"credential_finalization\": \"&lt;base64 OPAQUE CredentialFinalization&gt;\"\n}\n</code></pre> <p>Response:</p> <pre><code>{\n  \"encrypted_credentials\": \"&lt;base64 AES-GCM wrapped privateKey + symmetricRoot&gt;\",\n  \"public_key\": \"U...\"\n}\n</code></pre> <p>The client decrypts <code>encrypted_credentials</code> with the OPAQUE <code>export_key</code>, recovers <code>privateKey</code> and <code>symmetricRoot</code>, and uses them to authenticate via <code>/auth/challenge</code> and <code>/auth/verify</code>.</p>"},{"location":"reference/api/#common-status-codes","title":"Common status codes","text":"Code Meaning 200 OK 201 Created 204 No Content (delete success) 400 Bad Request \u2014 invalid input 401 Unauthorized \u2014 missing or invalid token 403 Forbidden \u2014 insufficient capability 404 Not Found 409 Conflict \u2014 hash chain conflict (<code>prev_hash</code> mismatch) 413 Payload Too Large \u2014 blob exceeds <code>max_blob_size</code> 429 Too Many Requests \u2014 OPAQUE rate limit 501 Not Implemented \u2014 feature not enabled (e.g., OPAQUE) 503 Service Unavailable \u2014 database temporarily unavailable"},{"location":"reference/python-sdk/","title":"Python SDK Reference","text":"<p>Package: <code>reeeductio</code> Install: <code>pip install reeeductio</code> Requires: Python \u2265 3.11</p> <p>The main entry point is the <code>Space</code> class. Import it with:</p> <pre><code>from reeeductio import Space\n</code></pre>"},{"location":"reference/python-sdk/#space","title":"<code>Space</code>","text":"<p>High-level client for interacting with a rEEEductio space.</p>"},{"location":"reference/python-sdk/#constructor","title":"Constructor","text":"<pre><code>Space(\n    space_id: str,\n    member_id: str,\n    private_key: bytes,\n    symmetric_root: bytes,\n    base_url: str = \"http://localhost:8000\",\n    auto_authenticate: bool = True,\n    local_store: LocalMessageStore | None = None,\n    user_symmetric_key: bytes | None = None,\n)\n</code></pre> Parameter Type Description <code>space_id</code> <code>str</code> 44-char space identifier (<code>S...</code>) <code>member_id</code> <code>str</code> Your user ID (<code>U...</code>) or tool ID (<code>T...</code>) <code>private_key</code> <code>bytes</code> 32-byte Ed25519 private key <code>symmetric_root</code> <code>bytes</code> 32-byte root encryption key <code>base_url</code> <code>str</code> Server URL (default: <code>http://localhost:8000</code>) <code>auto_authenticate</code> <code>bool</code> Authenticate automatically on first call (default: <code>True</code>) <code>local_store</code> <code>LocalMessageStore \\| None</code> Optional local message cache <code>user_symmetric_key</code> <code>bytes \\| None</code> Optional 32-byte user-private key for user-private encryption <p>Use as a context manager to ensure the HTTP client is closed: <pre><code>with Space(...) as space:\n    msgs = space.get_messages('general')\n</code></pre></p>"},{"location":"reference/python-sdk/#derived-attributes","title":"Derived attributes","text":"Attribute Type Description <code>space_id</code> <code>str</code> Space identifier <code>member_id</code> <code>str</code> This member's user/tool ID <code>message_key</code> <code>bytes</code> Derived message encryption key <code>state_key</code> <code>bytes</code> Derived state encryption key <code>data_key</code> <code>bytes</code> Derived data encryption key"},{"location":"reference/python-sdk/#authentication","title":"Authentication","text":""},{"location":"reference/python-sdk/#authenticate-str","title":"<code>authenticate() \u2192 str</code>","text":"<p>Perform challenge-response authentication. Returns a JWT token. Usually called automatically.</p> <pre><code>token = space.authenticate()\n</code></pre>"},{"location":"reference/python-sdk/#messages","title":"Messages","text":""},{"location":"reference/python-sdk/#post_messagetopic_id-msg_type-data-prev_hashnone-messagecreated","title":"<code>post_message(topic_id, msg_type, data, prev_hash=None) \u2192 MessageCreated</code>","text":"<p>Post raw (unencrypted) bytes to a topic.</p> <pre><code>result = space.post_message('general', 'chat.text', encrypted_bytes)\nprint(result.message_hash)  # M...\n</code></pre>"},{"location":"reference/python-sdk/#post_encrypted_messagetopic_id-msg_type-data-prev_hashnone-messagecreated","title":"<code>post_encrypted_message(topic_id, msg_type, data, prev_hash=None) \u2192 MessageCreated</code>","text":"<p>Encrypt <code>data</code> client-side with the topic key, then post it.</p> <pre><code>result = space.post_encrypted_message('general', 'chat.text', b'Hello!')\n</code></pre>"},{"location":"reference/python-sdk/#decrypt_message_datamsg-topic_id-bytes","title":"<code>decrypt_message_data(msg, topic_id) \u2192 bytes</code>","text":"<p>Decrypt the <code>data</code> field of a message returned by <code>get_messages</code>.</p> <pre><code>msgs = space.get_messages('general')\nfor msg in msgs:\n    plaintext = space.decrypt_message_data(msg, 'general')\n</code></pre>"},{"location":"reference/python-sdk/#get_messagestopic_id-from_timestampnone-to_timestampnone-limit100-listmessage","title":"<code>get_messages(topic_id, from_timestamp=None, to_timestamp=None, limit=100) \u2192 list[Message]</code>","text":"<p>Retrieve messages from a topic. Results are ordered by <code>server_timestamp</code> ascending.</p> <pre><code>msgs = space.get_messages('general', limit=50)\n</code></pre>"},{"location":"reference/python-sdk/#get_messagetopic_id-message_hash-use_cachetrue-message","title":"<code>get_message(topic_id, message_hash, use_cache=True) \u2192 Message</code>","text":"<p>Retrieve a single message by its hash.</p>"},{"location":"reference/python-sdk/#derive_topic_keytopic_id-bytes","title":"<code>derive_topic_key(topic_id) \u2192 bytes</code>","text":"<p>Derive the 32-byte encryption key for a topic. Rarely needed directly.</p>"},{"location":"reference/python-sdk/#state","title":"State","text":""},{"location":"reference/python-sdk/#get_plaintext_statepath-str","title":"<code>get_plaintext_state(path) \u2192 str</code>","text":"<p>Get the current value at a state path as a UTF-8 string.</p> <pre><code>profile = space.get_plaintext_state('profiles/alice')\n</code></pre>"},{"location":"reference/python-sdk/#get_encrypted_statepath-keynone-str","title":"<code>get_encrypted_state(path, key=None) \u2192 str</code>","text":"<p>Get and decrypt the value at a state path.</p>"},{"location":"reference/python-sdk/#set_plaintext_statepath-data-prev_hashnone-messagecreated","title":"<code>set_plaintext_state(path, data, prev_hash=None) \u2192 MessageCreated</code>","text":"<p>Write a UTF-8 string value to a state path.</p> <pre><code>space.set_plaintext_state('profiles/alice', '{\"name\": \"Alice\"}')\n</code></pre>"},{"location":"reference/python-sdk/#set_encrypted_statepath-data-prev_hashnone-keynone-messagecreated","title":"<code>set_encrypted_state(path, data, prev_hash=None, key=None) \u2192 MessageCreated</code>","text":"<p>Encrypt and write a value to a state path.</p>"},{"location":"reference/python-sdk/#get_state_historyfrom_timestampnone-to_timestampnone-limit100-listmessage","title":"<code>get_state_history(from_timestamp=None, to_timestamp=None, limit=100) \u2192 list[Message]</code>","text":"<p>Retrieve the full event log of state changes.</p>"},{"location":"reference/python-sdk/#blobs","title":"Blobs","text":""},{"location":"reference/python-sdk/#encrypt_and_upload_blobdata-encryptedblobcreated","title":"<code>encrypt_and_upload_blob(data) \u2192 EncryptedBlobCreated</code>","text":"<p>Encrypt <code>data</code> with a random per-blob DEK and upload it. Returns <code>blob_id</code> and <code>dek</code>.</p> <pre><code>result = space.encrypt_and_upload_blob(open('photo.jpg', 'rb').read())\n# Save result.blob_id and result.dek \u2014 you need both to download\n</code></pre>"},{"location":"reference/python-sdk/#download_and_decrypt_blobblob_id-key-bytes","title":"<code>download_and_decrypt_blob(blob_id, key) \u2192 bytes</code>","text":"<p>Download and decrypt a blob using its DEK.</p> <pre><code>data = space.download_and_decrypt_blob(blob_id, dek)\n</code></pre>"},{"location":"reference/python-sdk/#upload_plaintext_blobdata-blobcreated","title":"<code>upload_plaintext_blob(data) \u2192 BlobCreated</code>","text":"<p>Upload unencrypted bytes. Returns <code>blob_id</code>.</p>"},{"location":"reference/python-sdk/#download_plaintext_blobblob_id-bytes","title":"<code>download_plaintext_blob(blob_id) \u2192 bytes</code>","text":"<p>Download unencrypted bytes.</p>"},{"location":"reference/python-sdk/#delete_blobblob_id-none","title":"<code>delete_blob(blob_id) \u2192 None</code>","text":"<p>Delete a blob.</p>"},{"location":"reference/python-sdk/#data-kv-store","title":"Data (KV store)","text":""},{"location":"reference/python-sdk/#get_plaintext_datapath-bytes","title":"<code>get_plaintext_data(path) \u2192 bytes</code>","text":"<p>Get a value from the key-value store.</p>"},{"location":"reference/python-sdk/#get_encrypted_datapath-keynone-bytes","title":"<code>get_encrypted_data(path, key=None) \u2192 bytes</code>","text":"<p>Get and decrypt a value.</p>"},{"location":"reference/python-sdk/#set_plaintext_datapath-data-int","title":"<code>set_plaintext_data(path, data) \u2192 int</code>","text":"<p>Set a value. Returns the signed timestamp.</p>"},{"location":"reference/python-sdk/#set_encrypted_datapath-data-keynone-int","title":"<code>set_encrypted_data(path, data, key=None) \u2192 int</code>","text":"<p>Encrypt and set a value.</p>"},{"location":"reference/python-sdk/#authorization","title":"Authorization","text":""},{"location":"reference/python-sdk/#add_useruser_id-descriptionnone-messagecreated","title":"<code>add_user(user_id, description=None) \u2192 MessageCreated</code>","text":"<p>Register a user in the space (writes to <code>auth/users/{user_id}</code>).</p>"},{"location":"reference/python-sdk/#create_tooltool_id-descriptionnone-messagecreated","title":"<code>create_tool(tool_id, description=None) \u2192 MessageCreated</code>","text":"<p>Register a tool account.</p>"},{"location":"reference/python-sdk/#create_rolerole_name-descriptionnone-messagecreated","title":"<code>create_role(role_name, description=None) \u2192 MessageCreated</code>","text":"<p>Create a role definition.</p>"},{"location":"reference/python-sdk/#grant_capability_to_rolerole_name-cap_id-capability-messagecreated","title":"<code>grant_capability_to_role(role_name, cap_id, capability) \u2192 MessageCreated</code>","text":"<p>Grant a capability to a role. <code>capability</code> is <code>{\"op\": \"read|create|...\", \"path\": \"...\"}</code>.</p>"},{"location":"reference/python-sdk/#assign_role_to_useruser_id-role_name-messagecreated","title":"<code>assign_role_to_user(user_id, role_name) \u2192 MessageCreated</code>","text":"<p>Assign a role to a user.</p>"},{"location":"reference/python-sdk/#grant_capability_to_useruser_id-cap_id-capability-messagecreated","title":"<code>grant_capability_to_user(user_id, cap_id, capability) \u2192 MessageCreated</code>","text":"<p>Grant a direct capability to a user.</p>"},{"location":"reference/python-sdk/#grant_capability_to_tooltool_id-cap_id-capability-messagecreated","title":"<code>grant_capability_to_tool(tool_id, cap_id, capability) \u2192 MessageCreated</code>","text":"<p>Grant a capability to a tool.</p>"},{"location":"reference/python-sdk/#crypto-utilities","title":"Crypto utilities","text":"<pre><code>from reeeductio.crypto import (\n    generate_keypair,\n    to_space_id,\n    to_user_id,\n    to_tool_id,\n    string_to_bytes,\n    bytes_to_string,\n    encode_base64,\n    decode_base64,\n)\n</code></pre>"},{"location":"reference/python-sdk/#generate_keypair-tuplebytes-bytes","title":"<code>generate_keypair() \u2192 tuple[bytes, bytes]</code>","text":"<p>Returns <code>(private_key, public_key)</code> as 32-byte values.</p>"},{"location":"reference/python-sdk/#to_space_idpublic_key-str","title":"<code>to_space_id(public_key) \u2192 str</code>","text":"<p>Encode an Ed25519 public key as a space ID (<code>S...</code>).</p>"},{"location":"reference/python-sdk/#to_user_idpublic_key-str","title":"<code>to_user_id(public_key) \u2192 str</code>","text":"<p>Encode as a user ID (<code>U...</code>).</p>"},{"location":"reference/python-sdk/#to_tool_idpublic_key-str","title":"<code>to_tool_id(public_key) \u2192 str</code>","text":"<p>Encode as a tool ID (<code>T...</code>).</p>"},{"location":"reference/python-sdk/#exceptions","title":"Exceptions","text":"Exception When raised <code>AuthenticationError</code> Challenge-response or token refresh failed <code>AuthorizationError</code> Server returned 403 <code>NotFoundError</code> Resource does not exist (404) <code>ValidationError</code> Invalid request or data <code>ChainError</code> Message chain integrity check failed <code>BlobError</code> Blob upload or download failed <code>OpaqueError</code> OPAQUE protocol error <pre><code>from reeeductio.exceptions import NotFoundError, ChainError\n\ntry:\n    data = space.get_plaintext_state('profiles/alice')\nexcept NotFoundError:\n    print('No profile yet')\n</code></pre>"},{"location":"reference/python-sdk/#models","title":"Models","text":"<p>Key types returned by SDK methods:</p> Type Fields <code>Message</code> <code>message_hash</code>, <code>prev_hash</code>, <code>type</code>, <code>data</code>, <code>sender</code>, <code>signature</code>, <code>server_timestamp</code> <code>MessageCreated</code> <code>message_hash</code>, <code>server_timestamp</code> <code>BlobCreated</code> <code>blob_id</code>, <code>size</code> <code>EncryptedBlobCreated</code> <code>blob_id</code>, <code>size</code>, <code>dek</code> (32-byte DEK) <code>DataEntry</code> <code>path</code>, <code>data</code>, <code>signed_by</code>, <code>signature</code>, <code>timestamp</code>"},{"location":"reference/python-sdk/#cli","title":"CLI","text":"<p>The <code>reeeductio-admin</code> CLI tool is included with the package:</p> <pre><code>reeeductio-admin --help\n\n# Key commands\nreeeductio-admin space generate        # Generate new space credentials\nreeeductio-admin space create          # Register a space on the server\nreeeductio-admin user add U...         # Add a user\nreeeductio-admin user assign-role U... --role member\nreeeductio-admin role create member\nreeeductio-admin role grant member --cap-id read-all --op read --path \"topics/{any}\"\nreeeductio-admin tool add T...\nreeeductio-admin tool grant T... --cap-id post-alerts --op create --path \"topics/alerts/messages/{any}\"\n</code></pre>"},{"location":"reference/server-config/","title":"Server Configuration Reference","text":"<p>The rEEEductio server reads configuration from a YAML (or JSON) file. The path to the file can be set with the <code>CONFIG_FILE</code> environment variable, or passed directly to the application at startup.</p> <p>Any configuration value can be overridden with an environment variable using the pattern <code>SECTION__FIELD</code> (double underscore separator), for example <code>SERVER__PORT=9000</code>.</p>"},{"location":"reference/server-config/#quick-example","title":"Quick example","text":"<pre><code>environment: production\n\nserver:\n  host: 0.0.0.0\n  port: 8000\n  jwt_secret: change-me-in-production\n  jwt_expiry_hours: 24\n\ndatabase:\n  type: sqlite\n\nblob_store:\n  type: filesystem\n  path: /data/blobs\n\nlogging:\n  level: INFO\n  format: json\n\nadmin:\n  space_id: S...\n  auto_create_spaces: false\n</code></pre>"},{"location":"reference/server-config/#top-level-options","title":"Top-level options","text":"Key Type Default Description <code>environment</code> <code>string</code> <code>development</code> <code>development</code>, <code>production</code>, or <code>test</code> <code>debug</code> <code>bool</code> <code>false</code> Enable debug mode (verbose error responses)"},{"location":"reference/server-config/#server","title":"<code>server:</code>","text":"<p>General HTTP server settings.</p> Key Type Default Description <code>host</code> <code>string</code> <code>0.0.0.0</code> Network interface to bind to <code>port</code> <code>integer</code> <code>8000</code> TCP port to listen on <code>jwt_secret</code> <code>string</code> (random) Secret used to sign JWT tokens. Set this explicitly in production. <code>jwt_algorithm</code> <code>string</code> <code>HS256</code> JWT signing algorithm <code>jwt_expiry_hours</code> <code>integer</code> <code>24</code> Token lifetime in hours <code>challenge_expiry_seconds</code> <code>integer</code> <code>300</code> How long an auth challenge remains valid <code>max_message_size</code> <code>integer</code> <code>102400</code> Maximum message payload in bytes (100 KB) <code>max_blob_size</code> <code>integer</code> <code>104857600</code> Maximum blob size in bytes (100 MB) <p>Set <code>jwt_secret</code> in production</p> <p>If <code>jwt_secret</code> is not set, a random secret is generated at startup. Tokens issued before a restart become invalid. Always set an explicit secret in production.</p>"},{"location":"reference/server-config/#database","title":"<code>database:</code>","text":"<p>Configures where messages and state are stored.</p>"},{"location":"reference/server-config/#sqlite-default","title":"SQLite (default)","text":"<pre><code>database:\n  type: sqlite\n</code></pre> Key Type Default Description <code>type</code> <code>string</code> \u2014 Must be <code>sqlite</code> <p>SQLite databases are created automatically per space under the <code>spaces/</code> directory managed by the server. There are no path settings to configure.</p>"},{"location":"reference/server-config/#firestore","title":"Firestore","text":"<pre><code>database:\n  type: firestore\n  project_id: my-gcp-project\n  database_id: (default)\n</code></pre> Key Type Default Description <code>type</code> <code>string</code> \u2014 Must be <code>firestore</code> <code>project_id</code> <code>string</code> (from ADC) GCP project ID. Omit to use Application Default Credentials. <code>database_id</code> <code>string</code> <code>(default)</code> Firestore database ID <p>Firestore requires Google Application Default Credentials. In Cloud Run this is automatic; elsewhere set <code>GOOGLE_APPLICATION_CREDENTIALS</code> to a service account key file.</p>"},{"location":"reference/server-config/#blob_store","title":"<code>blob_store:</code>","text":"<p>Configures where uploaded blobs are stored.</p>"},{"location":"reference/server-config/#filesystem-default","title":"Filesystem (default)","text":"<pre><code>blob_store:\n  type: filesystem\n  path: blobs\n</code></pre> Key Type Default Description <code>type</code> <code>string</code> \u2014 Must be <code>filesystem</code> <code>path</code> <code>string</code> <code>blobs</code> Directory where blob files are stored"},{"location":"reference/server-config/#s3-s3-compatible","title":"S3 / S3-compatible","text":"<pre><code>blob_store:\n  type: s3\n  bucket_name: my-bucket\n  region_name: us-east-1\n  # For MinIO or other S3-compatible stores:\n  endpoint_url: http://minio:9000\n  public_endpoint_url: https://minio.example.com\n  access_key_id: AKIAIOSFODNN7EXAMPLE\n  secret_access_key: wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY\n  presigned_url_expiration: 3600\n</code></pre> Key Type Default Description <code>type</code> <code>string</code> \u2014 Must be <code>s3</code> <code>bucket_name</code> <code>string</code> (required) S3 bucket name <code>region_name</code> <code>string</code> <code>us-east-1</code> AWS region <code>endpoint_url</code> <code>string</code> (AWS) Custom endpoint for MinIO or Backblaze B2 <code>public_endpoint_url</code> <code>string</code> (same as endpoint_url) Public-facing endpoint used in presigned URLs (useful when the server connects via internal DNS but clients need the public hostname) <code>access_key_id</code> <code>string</code> (from IAM) AWS access key. Omit to use IAM instance role. <code>secret_access_key</code> <code>string</code> (from IAM) AWS secret key <code>presigned_url_expiration</code> <code>integer</code> <code>3600</code> Presigned URL lifetime in seconds"},{"location":"reference/server-config/#sqlite-blob-store","title":"SQLite blob store","text":"<pre><code>blob_store:\n  type: sqlite\n  db_path: blobs.db\n</code></pre> <p>Stores blobs as BLOBs inside a SQLite database. Convenient for testing; not recommended for use outside of testing.</p>"},{"location":"reference/server-config/#logging","title":"<code>logging:</code>","text":"Key Type Default Description <code>level</code> <code>string</code> <code>INFO</code> Log level: <code>DEBUG</code>, <code>INFO</code>, <code>WARNING</code>, <code>ERROR</code>, <code>CRITICAL</code> <code>format</code> <code>string</code> <code>text</code> Output format: <code>text</code> (human-readable) or <code>json</code> (structured, for log aggregators) <code>file</code> <code>string</code> (stdout only) Path to a log file. Enables rotating file logging in addition to stdout. <code>max_bytes</code> <code>integer</code> <code>10485760</code> Maximum log file size before rotation (10 MB) <code>backup_count</code> <code>integer</code> <code>5</code> Number of rotated log files to keep <code>enable_access_log</code> <code>bool</code> <code>true</code> Enable uvicorn HTTP access logs"},{"location":"reference/server-config/#admin","title":"<code>admin:</code>","text":"<p>The admin space is an optional special space that the server itself has an account in. It is used for administrative operations such as provisioning new spaces.</p> Key Type Default Description <code>space_id</code> <code>string</code> (none) Admin space ID (<code>S...</code>). Required to enable admin API endpoints. <code>user_id</code> <code>string</code> (none) Admin user ID (<code>U...</code>) for server-initiated state writes <code>private_key</code> <code>string</code> (none) Base64-encoded 32-byte Ed25519 private key for the admin user <code>auto_create_spaces</code> <code>bool</code> <code>false</code> If <code>true</code>, any client can create a space by posting to <code>/spaces/{space_id}</code> without prior admin authorization. Set to <code>false</code> in production."},{"location":"reference/server-config/#setting-up-the-admin-space","title":"Setting up the admin space","text":"<ol> <li> <p>Generate admin space credentials:</p> <pre><code>reeeductio-admin space generate &gt; admin-creds.json\n</code></pre> </li> <li> <p>Copy the <code>space_id</code> into <code>admin.space_id</code> in your config file.</p> </li> <li> <p>Start the server. On first startup it creates the admin space and registers the admin user automatically.</p> </li> </ol>"},{"location":"reference/server-config/#environment-variable-reference","title":"Environment variable reference","text":"<p>All config keys are available as environment variables using <code>SECTION__KEY</code> format:</p> <pre><code>SERVER__JWT_SECRET=my-secret\nSERVER__PORT=9000\nDATABASE__TYPE=firestore\nDATABASE__PROJECT_ID=my-project\nBLOB_STORE__TYPE=s3\nBLOB_STORE__BUCKET_NAME=my-bucket\nBLOB_STORE__ENDPOINT_URL=http://minio:9000\nLOGGING__LEVEL=DEBUG\nLOGGING__FORMAT=json\nADMIN__SPACE_ID=S...\nADMIN__AUTO_CREATE_SPACES=false\nENVIRONMENT=production\nDEBUG=false\n</code></pre> <p>Environment variables take precedence over values in the config file.</p>"},{"location":"reference/server-config/#docker","title":"Docker","text":""},{"location":"reference/server-config/#minimal-docker-composeyml","title":"Minimal <code>docker-compose.yml</code>","text":"<pre><code>services:\n  server:\n    image: reeeductio/server:latest\n    ports:\n      - \"8000:8000\"\n    volumes:\n      - ./data:/data\n    environment:\n      CONFIG_FILE: /data/config.yaml\n</code></pre>"},{"location":"reference/server-config/#recommended-production-layout","title":"Recommended production layout","text":"<pre><code>/data/\n  config.yaml        \u2190 server configuration\n  spaces/            \u2190 per-space SQLite databases (created automatically)\n    S.../\n      data.db\n      messages.db\n  blobs/             \u2190 blob storage directory\n</code></pre> <p>For persistence, mount <code>/data</code> as a Docker volume.</p>"},{"location":"reference/server-config/#horizontal-scaling","title":"Horizontal Scaling","text":"<p>Multiple rEEEductio instances can run behind a load balancer. The right strategy depends on which database backend you use.</p>"},{"location":"reference/server-config/#sqlite-consistent-hashing","title":"SQLite: consistent hashing","text":"<p>Because each space's data lives in its own SQLite database files under <code>spaces/{space_id}/</code>, concurrent writes from two different server instances to the same space would require file-level coordination that SQLite is not designed for. The solution is to ensure all requests for a given space always reach the same instance \u2014 then SQLite's single-writer model works perfectly.</p> <p>Configure your reverse proxy to use consistent hashing on the space ID as the routing key. With Nginx:</p> <pre><code>upstream reeeductio {\n    hash $space_id consistent;\n    server instance1:8000;\n    server instance2:8000;\n    server instance3:8000;\n}\n\nserver {\n    location ~ ^/spaces/([^/]+) {\n        set $space_id $1;\n        proxy_pass http://reeeductio;\n    }\n    # Admin and health endpoints can go to any instance\n    location / {\n        proxy_pass http://reeeductio;\n    }\n}\n</code></pre> <p>Each instance needs access to the shared <code>spaces/</code> directory, typically via a network filesystem (NFS, EFS, etc.) or by running each instance on the same host with a shared volume. Because each instance owns a disjoint subset of spaces, there is no contention on the shared filesystem.</p> <p>WebSocket connections</p> <p>WebSocket (<code>/spaces/{space_id}/stream</code>) connections must also be routed to the same instance as regular HTTP requests for that space, since the in-process broadcast mechanism is per-instance. Consistent hashing on the space ID handles this automatically.</p>"},{"location":"reference/server-config/#firestore-stateless-horizontal-scaling","title":"Firestore: stateless horizontal scaling","text":"<p>With <code>database.type: firestore</code>, all instances share the same Firestore database and are fully stateless. You can use any load balancing strategy \u2014 round-robin, least-connections, etc. \u2014 without sticky sessions or consistent hashing.</p>"},{"location":"reference/server-config/#blob-storage","title":"Blob storage","text":"<p>Blob storage must also be shared. Use <code>blob_store.type: s3</code> so all instances read and write the same bucket.</p> <pre><code>database:\n  type: firestore\n  project_id: my-gcp-project\n\nblob_store:\n  type: s3\n  bucket_name: my-blobs\n</code></pre> <p>This is the recommended configuration for Cloud Run or any auto-scaling environment.</p>"},{"location":"reference/typescript-sdk/","title":"TypeScript SDK Reference","text":"<p>Package: <code>@reeeductio/client</code> Requires: Node.js \u2265 18 or a modern browser (Web Crypto API required)</p> <p>The main entry point is the <code>Space</code> class:</p> <pre><code>import { Space } from '@reeeductio/client';\n</code></pre>"},{"location":"reference/typescript-sdk/#space","title":"<code>Space</code>","text":"<p>High-level client for interacting with a rEEEductio space.</p>"},{"location":"reference/typescript-sdk/#constructor","title":"Constructor","text":"<pre><code>new Space({\n  spaceId: string,\n  keyPair: KeyPair,\n  symmetricRoot: Uint8Array,\n  baseUrl?: string,\n  fetch?: typeof fetch,\n  localStore?: MessageStore,\n})\n</code></pre> Parameter Type Description <code>spaceId</code> <code>string</code> 44-char space identifier (<code>S...</code>) <code>keyPair</code> <code>KeyPair</code> Ed25519 key pair for signing <code>symmetricRoot</code> <code>Uint8Array</code> 32-byte root encryption key <code>baseUrl</code> <code>string</code> Server URL (default: <code>http://localhost:8000</code>) <code>fetch</code> <code>typeof fetch</code> Custom fetch implementation (Node.js or browser) <code>localStore</code> <code>MessageStore \\| undefined</code> Optional local message cache <pre><code>const space = new Space({\n  spaceId: 'S...',\n  keyPair: { privateKey, publicKey },\n  symmetricRoot,\n  baseUrl: 'https://my-server.example.com',\n});\n</code></pre>"},{"location":"reference/typescript-sdk/#derived-attributes","title":"Derived attributes","text":"Attribute Type Description <code>spaceId</code> <code>string</code> Space identifier <code>keyPair</code> <code>KeyPair</code> This member's key pair <code>messageKey</code> <code>Uint8Array</code> Derived message encryption key <code>stateKey</code> <code>Uint8Array</code> Derived state encryption key <code>dataKey</code> <code>Uint8Array</code> Derived data encryption key"},{"location":"reference/typescript-sdk/#getuserid-string","title":"<code>getUserId() \u2192 string</code>","text":"<p>Returns the user ID derived from this space's key pair.</p> <pre><code>const userId = space.getUserId(); // 'U...'\n</code></pre>"},{"location":"reference/typescript-sdk/#authentication","title":"Authentication","text":""},{"location":"reference/typescript-sdk/#authenticate-promisestring","title":"<code>authenticate() \u2192 Promise&lt;string&gt;</code>","text":"<p>Perform challenge-response authentication. Returns a JWT token. Usually called automatically on the first API call.</p> <pre><code>const token = await space.authenticate();\n</code></pre>"},{"location":"reference/typescript-sdk/#static-spacefromopaqueloginoptions-promisespace","title":"<code>static Space.fromOpaqueLogin(options) \u2192 Promise&lt;Space&gt;</code>","text":"<p>Create a <code>Space</code> by logging in with a username and password via OPAQUE.</p> <pre><code>const space = await Space.fromOpaqueLogin({\n  baseUrl: 'https://my-server.example.com',\n  spaceId: 'S...',\n  username: 'alice',\n  password: 'my-password',\n});\n</code></pre>"},{"location":"reference/typescript-sdk/#messages","title":"Messages","text":""},{"location":"reference/typescript-sdk/#postmessagetopicid-msgtype-data-prevhash-promisemessagecreated","title":"<code>postMessage(topicId, msgType, data, prevHash?) \u2192 Promise&lt;MessageCreated&gt;</code>","text":"<p>Post raw (unencrypted) bytes to a topic.</p> <pre><code>const result = await space.postMessage('general', 'chat.text', encryptedBytes);\nconsole.log(result.message_hash); // M...\n</code></pre>"},{"location":"reference/typescript-sdk/#postencryptedmessagetopicid-msgtype-plaintext-prevhash-promisemessagecreated","title":"<code>postEncryptedMessage(topicId, msgType, plaintext, prevHash?) \u2192 Promise&lt;MessageCreated&gt;</code>","text":"<p>Encrypt <code>plaintext</code> client-side with the topic key, then post it.</p> <pre><code>const encoder = new TextEncoder();\nconst result = await space.postEncryptedMessage(\n  'general',\n  'chat.text',\n  encoder.encode('Hello!')\n);\n</code></pre>"},{"location":"reference/typescript-sdk/#decryptmessagedatamsg-topicid-uint8array","title":"<code>decryptMessageData(msg, topicId) \u2192 Uint8Array</code>","text":"<p>Decrypt the <code>data</code> field of a message returned by <code>getMessages</code>.</p> <pre><code>const messages = await space.getMessages('general');\nfor (const msg of messages.messages) {\n  const plaintext = space.decryptMessageData(msg, 'general');\n  console.log(new TextDecoder().decode(plaintext));\n}\n</code></pre>"},{"location":"reference/typescript-sdk/#getmessagestopicid-query-options-promisemessagesresponse","title":"<code>getMessages(topicId, query?, options?) \u2192 Promise&lt;MessagesResponse&gt;</code>","text":"<p>Retrieve messages from a topic. Results are ordered by <code>server_timestamp</code> ascending.</p> <pre><code>const result = await space.getMessages('general', { limit: 50 });\nfor (const msg of result.messages) {\n  const text = space.decryptMessageData(msg, 'general');\n}\n</code></pre> Query field Type Description <code>from</code> <code>number</code> Start timestamp (ms) <code>to</code> <code>number</code> End timestamp (ms) <code>limit</code> <code>number</code> Max messages (default 100, max 1000)"},{"location":"reference/typescript-sdk/#getmessagetopicid-messagehash-options-promisemessage","title":"<code>getMessage(topicId, messageHash, options?) \u2192 Promise&lt;Message&gt;</code>","text":"<p>Retrieve a single message by its hash.</p> <pre><code>const msg = await space.getMessage('general', 'M...');\n</code></pre>"},{"location":"reference/typescript-sdk/#derivetopickeytopicid-uint8array","title":"<code>deriveTopicKey(topicId) \u2192 Uint8Array</code>","text":"<p>Derive the 32-byte encryption key for a topic. Rarely needed directly.</p>"},{"location":"reference/typescript-sdk/#state","title":"State","text":""},{"location":"reference/typescript-sdk/#getplaintextstatepath-promiseuint8array","title":"<code>getPlaintextState(path) \u2192 Promise&lt;Uint8Array&gt;</code>","text":"<p>Get the current value at a state path as raw bytes.</p> <pre><code>const data = await space.getPlaintextState('profiles/alice');\nconst profile = JSON.parse(new TextDecoder().decode(data));\n</code></pre>"},{"location":"reference/typescript-sdk/#getencryptedstatepath-promiseuint8array","title":"<code>getEncryptedState(path) \u2192 Promise&lt;Uint8Array&gt;</code>","text":"<p>Get and decrypt the value at a state path.</p>"},{"location":"reference/typescript-sdk/#setplaintextstatepath-data-prevhash-promisemessagecreated","title":"<code>setPlaintextState(path, data, prevHash?) \u2192 Promise&lt;MessageCreated&gt;</code>","text":"<p>Write bytes to a state path.</p> <pre><code>const encoder = new TextEncoder();\nawait space.setPlaintextState(\n  'profiles/alice',\n  encoder.encode(JSON.stringify({ name: 'Alice' }))\n);\n</code></pre>"},{"location":"reference/typescript-sdk/#setencryptedstatepath-data-prevhash-promisemessagecreated","title":"<code>setEncryptedState(path, data, prevHash?) \u2192 Promise&lt;MessageCreated&gt;</code>","text":"<p>Encrypt and write a value to a state path.</p>"},{"location":"reference/typescript-sdk/#getstatehistoryquery-promisemessagesresponse","title":"<code>getStateHistory(query?) \u2192 Promise&lt;MessagesResponse&gt;</code>","text":"<p>Retrieve the full event log of state changes.</p>"},{"location":"reference/typescript-sdk/#blobs","title":"Blobs","text":""},{"location":"reference/typescript-sdk/#encryptanduploadblobdata-associateddata-promiseencryptedblobcreated","title":"<code>encryptAndUploadBlob(data, associatedData?) \u2192 Promise&lt;EncryptedBlobCreated&gt;</code>","text":"<p>Encrypt <code>data</code> with a random per-blob DEK and upload it. Returns <code>blob_id</code> and <code>key</code> (the DEK).</p> <pre><code>const result = await space.encryptAndUploadBlob(fileBytes);\n// Save result.blob_id and result.key \u2014 you need both to download\n</code></pre>"},{"location":"reference/typescript-sdk/#downloadanddecryptblobblobid-key-associateddata-promiseuint8array","title":"<code>downloadAndDecryptBlob(blobId, key, associatedData?) \u2192 Promise&lt;Uint8Array&gt;</code>","text":"<p>Download and decrypt a blob using its DEK.</p> <pre><code>const data = await space.downloadAndDecryptBlob(blobId, dek);\n</code></pre>"},{"location":"reference/typescript-sdk/#uploadplaintextblobdata-promiseblobcreated","title":"<code>uploadPlaintextBlob(data) \u2192 Promise&lt;BlobCreated&gt;</code>","text":"<p>Upload unencrypted bytes. Returns <code>blob_id</code>.</p>"},{"location":"reference/typescript-sdk/#downloadplaintextblobblobid-promiseuint8array","title":"<code>downloadPlaintextBlob(blobId) \u2192 Promise&lt;Uint8Array&gt;</code>","text":"<p>Download unencrypted bytes.</p>"},{"location":"reference/typescript-sdk/#deleteblobblobid-promisevoid","title":"<code>deleteBlob(blobId) \u2192 Promise&lt;void&gt;</code>","text":"<p>Delete a blob.</p>"},{"location":"reference/typescript-sdk/#data-kv-store","title":"Data (KV store)","text":""},{"location":"reference/typescript-sdk/#getplaintextdatapath-promiseuint8array","title":"<code>getPlaintextData(path) \u2192 Promise&lt;Uint8Array&gt;</code>","text":"<p>Get a value from the key-value store.</p>"},{"location":"reference/typescript-sdk/#getencrypteddatapath-promiseuint8array","title":"<code>getEncryptedData(path) \u2192 Promise&lt;Uint8Array&gt;</code>","text":"<p>Get and decrypt a value.</p>"},{"location":"reference/typescript-sdk/#setplaintextdatapath-data-promisedatasetresponse","title":"<code>setPlaintextData(path, data) \u2192 Promise&lt;DataSetResponse&gt;</code>","text":"<p>Set a value. Returns the response with a signed timestamp.</p>"},{"location":"reference/typescript-sdk/#setencrypteddatapath-data-promisedatasetresponse","title":"<code>setEncryptedData(path, data) \u2192 Promise&lt;DataSetResponse&gt;</code>","text":"<p>Encrypt and set a value.</p>"},{"location":"reference/typescript-sdk/#authorization","title":"Authorization","text":""},{"location":"reference/typescript-sdk/#createuseruserid-description-prevhash-promisemessagecreated","title":"<code>createUser(userId, description?, prevHash?) \u2192 Promise&lt;MessageCreated&gt;</code>","text":"<p>Register a user in the space (writes to <code>auth/users/{userId}</code>).</p>"},{"location":"reference/typescript-sdk/#createtooldescription-options-promisetoolcreated","title":"<code>createTool(description, options?) \u2192 Promise&lt;ToolCreated&gt;</code>","text":"<p>Register a tool account. Returns a <code>ToolCreated</code> with the tool's <code>keyPair</code>.</p> <pre><code>const { keyPair, tool } = await space.createTool('CI/CD poster');\n// Save keyPair.privateKey securely\n</code></pre>"},{"location":"reference/typescript-sdk/#createrolerolename-description-prevhash-promisemessagecreated","title":"<code>createRole(roleName, description?, prevHash?) \u2192 Promise&lt;MessageCreated&gt;</code>","text":"<p>Create a role definition.</p>"},{"location":"reference/typescript-sdk/#grantcapabilitytorolerolename-capid-capability-prevhash-promisemessagecreated","title":"<code>grantCapabilityToRole(roleName, capId, capability, prevHash?) \u2192 Promise&lt;MessageCreated&gt;</code>","text":"<p>Grant a capability to a role. <code>capability</code> is <code>{ op: 'read' | 'create' | ..., path: '...' }</code>.</p>"},{"location":"reference/typescript-sdk/#assignroletouseruserid-rolename-prevhash-promisemessagecreated","title":"<code>assignRoleToUser(userId, roleName, prevHash?) \u2192 Promise&lt;MessageCreated&gt;</code>","text":"<p>Assign a role to a user.</p>"},{"location":"reference/typescript-sdk/#grantcapabilitytouseruserid-capid-capability-prevhash-promisemessagecreated","title":"<code>grantCapabilityToUser(userId, capId, capability, prevHash?) \u2192 Promise&lt;MessageCreated&gt;</code>","text":"<p>Grant a direct capability to a user.</p>"},{"location":"reference/typescript-sdk/#grantcapabilitytotooltoolid-capid-capability-prevhash-promisemessagecreated","title":"<code>grantCapabilityToTool(toolId, capId, capability, prevHash?) \u2192 Promise&lt;MessageCreated&gt;</code>","text":"<p>Grant a capability to a tool.</p>"},{"location":"reference/typescript-sdk/#websocket","title":"WebSocket","text":""},{"location":"reference/typescript-sdk/#getwebsocketconnectionurl-promisestring","title":"<code>getWebSocketConnectionUrl() \u2192 Promise&lt;string&gt;</code>","text":"<p>Get an authenticated WebSocket URL. The token is embedded in the URL query string.</p> <pre><code>const wsUrl = await space.getWebSocketConnectionUrl();\nconst ws = new WebSocket(wsUrl);\nws.onmessage = (event) =&gt; {\n  const msg = JSON.parse(event.data) as Message;\n  const plaintext = space.decryptMessageData(msg, msg.topic_id);\n};\n</code></pre>"},{"location":"reference/typescript-sdk/#handleincomingmessagemessage-promisevoid","title":"<code>handleIncomingMessage(message) \u2192 Promise&lt;void&gt;</code>","text":"<p>Process and cache an incoming WebSocket message in the local store.</p>"},{"location":"reference/typescript-sdk/#crypto-utilities","title":"Crypto utilities","text":"<pre><code>import {\n  generateKeyPair,\n  toSpaceId,\n  toUserId,\n  toToolId,\n  stringToBytes,\n  bytesToString,\n  encodeBase64,\n  decodeBase64,\n  encodeUrlSafeBase64,\n  decodeUrlSafeBase64,\n  deriveKey,\n} from '@reeeductio/client';\n</code></pre>"},{"location":"reference/typescript-sdk/#generatekeypair-promisekeypair","title":"<code>generateKeyPair() \u2192 Promise&lt;KeyPair&gt;</code>","text":"<p>Returns <code>{ privateKey, publicKey }</code> as 32-byte <code>Uint8Array</code> values.</p>"},{"location":"reference/typescript-sdk/#tospaceidpublickey-string","title":"<code>toSpaceId(publicKey) \u2192 string</code>","text":"<p>Encode an Ed25519 public key as a space ID (<code>S...</code>).</p>"},{"location":"reference/typescript-sdk/#touseridpublickey-string","title":"<code>toUserId(publicKey) \u2192 string</code>","text":"<p>Encode as a user ID (<code>U...</code>).</p>"},{"location":"reference/typescript-sdk/#totoolidpublickey-string","title":"<code>toToolId(publicKey) \u2192 string</code>","text":"<p>Encode as a tool ID (<code>T...</code>).</p>"},{"location":"reference/typescript-sdk/#derivekeyrootkey-info-length-uint8array","title":"<code>deriveKey(rootKey, info, length?) \u2192 Uint8Array</code>","text":"<p>Derive a key using HKDF-SHA256.</p>"},{"location":"reference/typescript-sdk/#opaque-password-based-key-recovery","title":"OPAQUE (password-based key recovery)","text":""},{"location":"reference/typescript-sdk/#opaqueregisterusername-password-promiseopaqueregistrationresult","title":"<code>opaqueRegister(username, password) \u2192 Promise&lt;OpaqueRegistrationResult&gt;</code>","text":"<p>Register OPAQUE credentials for the current user.</p>"},{"location":"reference/typescript-sdk/#enableopaque-promiseenableopaqueresult","title":"<code>enableOpaque() \u2192 Promise&lt;EnableOpaqueResult&gt;</code>","text":"<p>Enable OPAQUE for this space (admin only).</p>"},{"location":"reference/typescript-sdk/#exceptions","title":"Exceptions","text":"Exception When raised <code>AuthenticationError</code> Challenge-response or token refresh failed <code>AuthorizationError</code> Server returned 403 <code>NotFoundError</code> Resource does not exist (404) <code>ValidationError</code> Invalid request or data (400) <code>ChainError</code> Message chain conflict (409) <code>BlobError</code> Blob upload or download failed <code>OpaqueError</code> OPAQUE protocol error <code>OpaqueNotEnabledError</code> OPAQUE not enabled on server (501) <code>OpaqueRateLimitError</code> Too many OPAQUE attempts (429) <code>StreamError</code> WebSocket stream error <pre><code>import { NotFoundError, ChainError } from '@reeeductio/client';\n\ntry {\n  const data = await space.getPlaintextState('profiles/alice');\n} catch (e) {\n  if (e instanceof NotFoundError) {\n    console.log('No profile yet');\n  }\n}\n</code></pre>"},{"location":"reference/typescript-sdk/#models","title":"Models","text":"<p>Key types returned by SDK methods:</p> Type Fields <code>Message</code> <code>message_hash</code>, <code>topic_id</code>, <code>prev_hash</code>, <code>type</code>, <code>data</code>, <code>sender</code>, <code>signature</code>, <code>server_timestamp</code> <code>MessagesResponse</code> <code>messages: Message[]</code>, <code>has_more: boolean</code> <code>MessageCreated</code> <code>message_hash</code>, <code>server_timestamp</code> <code>BlobCreated</code> <code>blob_id</code>, <code>size</code> <code>EncryptedBlobCreated</code> <code>blob_id</code>, <code>size</code>, <code>key</code> (32-byte DEK as <code>Uint8Array</code>) <code>DataSetResponse</code> <code>path</code>, <code>signed_at</code> <code>KeyPair</code> <code>privateKey: Uint8Array</code>, <code>publicKey: Uint8Array</code> <code>Capability</code> <code>op: CapabilityOp</code>, <code>path: string</code> <code>ToolCreated</code> <code>tool_id</code>, <code>keyPair</code>, <code>description</code>"},{"location":"reference/typescript-sdk/#capabilityop","title":"<code>CapabilityOp</code>","text":"<pre><code>type CapabilityOp = 'read' | 'create' | 'modify' | 'delete' | 'write';\n</code></pre>"},{"location":"reference/typescript-sdk/#local-message-store","title":"Local message store","text":"<p>The SDK ships two <code>MessageStore</code> implementations for offline-first caching:</p> Class Environment Backend <code>InMemoryMessageStore</code> Node.js or browser In-memory map <code>IndexedDBMessageStore</code> Browser only IndexedDB <pre><code>import { Space, InMemoryMessageStore } from '@reeeductio/client';\n\nconst store = new InMemoryMessageStore();\nconst space = new Space({ ..., localStore: store });\n</code></pre>"},{"location":"security/internals/","title":"Security Internals","text":"<p>This page is for security-curious developers who want to understand the cryptographic design in detail.</p>"},{"location":"security/internals/#identity-and-key-material","title":"Identity and key material","text":"<p>Every participant (space, user, or tool) is an Ed25519 keypair. The public key is encoded as a typed identifier:</p> Prefix Type Derivation <code>S</code> Space ID <code>base64url(0b010010 \u2016 public_key)</code> <code>U</code> User ID <code>base64url(0b010101 \u2016 public_key)</code> <code>T</code> Tool ID <code>base64url(0b010011 \u2016 public_key)</code> <p>All identifiers are 44 URL-safe base64 characters (1 type byte + 32 key bytes = 33 bytes \u2192 44 chars).</p> <p>The space's public key is the root of trust \u2014 it determines the space ID, and the holder of the corresponding private key is the implicit admin with full authority.</p>"},{"location":"security/internals/#key-derivation-tree","title":"Key derivation tree","text":"<p>The <code>symmetricRoot</code> is a 32-byte random secret that is the root of all encryption in the space. Keys are derived with HKDF-SHA256 and an <code>info</code> string that encodes the key's purpose and scope, preventing key reuse across contexts:</p> <pre><code>symmetricRoot  (32 bytes, random)\n\u2502\n\u251c\u2500 HKDF(\"message key | {spaceId}\")  \u2192  message_key  (32 bytes)\n\u2502   \u2502\n\u2502   \u251c\u2500 HKDF(\"topic key | general\")  \u2192  topic_key[\"general\"]\n\u2502   \u251c\u2500 HKDF(\"topic key | alerts\")   \u2192  topic_key[\"alerts\"]\n\u2502   \u2514\u2500 HKDF(\"topic key | state\")    \u2192  state_key  (reserved)\n\u2502\n\u2514\u2500 HKDF(\"data key | {spaceId}\")     \u2192  data_key  (32 bytes)\n</code></pre> <p>The <code>spaceId</code> in the <code>info</code> string provides domain separation \u2014 the same <code>symmetricRoot</code> in two different spaces produces completely different keys, so sharing a root across spaces (which you shouldn't do) still doesn't allow cross-space decryption.</p>"},{"location":"security/internals/#encryption","title":"Encryption","text":"<p>All data is encrypted with AES-256-GCM:</p> <ul> <li>Key: 32-byte key from the derivation tree above</li> <li>Nonce: 12-byte random IV, prepended to the ciphertext</li> <li>Tag: 16-byte authentication tag, appended to the ciphertext</li> <li>Wire format: <code>IV (12 bytes) \u2016 ciphertext \u2016 tag (16 bytes)</code>, base64-encoded</li> </ul> <p>AES-GCM provides both confidentiality and integrity \u2014 a corrupted or tampered ciphertext will fail to decrypt.</p>"},{"location":"security/internals/#message-integrity-hash-chains","title":"Message integrity: hash chains","text":"<p>Every message in a topic has:</p> <ul> <li><code>message_hash</code>: SHA-256 of <code>{spaceId}|{topicId}|{prevHash}|{type}|{data}|{sender}|{signature}</code></li> <li><code>prev_hash</code>: the <code>message_hash</code> of the previous message (or <code>null</code> for the first)</li> <li><code>signature</code>: Ed25519 signature by the sender over the message hash</li> </ul> <p>This forms a tamper-evident append-only log:</p> <ul> <li>Any modification to a message changes its hash, which invalidates all subsequent <code>prev_hash</code> links.</li> <li>Any inserted or deleted message breaks the chain.</li> <li>Each message is independently verifiable against the sender's public key.</li> </ul> <p>The server enforces chain integrity with compare-and-swap at write time: a message is only accepted if its <code>prev_hash</code> matches the current chain head, atomically within a database transaction. This prevents forking even under concurrent writes.</p>"},{"location":"security/internals/#state-integrity-event-sourced","title":"State integrity: event-sourced","text":"<p>State is stored as messages in a reserved topic (<code>\"state\"</code>), using the same hash chain mechanism as regular topics. The state topic uses <code>state_key</code> (derived from <code>message_key</code>) for encryption.</p> <p>Each state write records the path being updated in the <code>type</code> field. The current value at any path is the data from the most recent message whose <code>type</code> matches that path.</p> <p>Because state is backed by a message chain, the full history of every state change is cryptographically verifiable. A server cannot substitute a forged state entry without breaking the chain.</p>"},{"location":"security/internals/#blob-integrity-content-addressing","title":"Blob integrity: content addressing","text":"<p>Blob IDs are derived from the hash of the encrypted content. If the server returns different bytes for the same blob ID, the hash check fails. This prevents substitution attacks.</p> <p>For encrypted blobs, each blob has its own randomly-generated 32-byte data encryption key (DEK). The DEK is returned to the uploader and must be stored separately \u2014 typically encrypted inside a message. The server never has the DEK.</p>"},{"location":"security/internals/#authorization-capability-chain-of-trust","title":"Authorization: capability chain of trust","text":"<p>Capabilities are stored as signed state entries. The authorization chain works as follows:</p> <ol> <li>Space creator (the keypair encoded in the space ID) has implicit full authority \u2014 no capability entry needed.</li> <li>The creator can write capability entries granting rights to users or roles.</li> <li>Users and tools can only be granted capabilities that are a subset of the grantor's own capabilities. The server enforces this: you cannot grant more than you have.</li> <li>Every capability entry is signed by the grantor. The server verifies the signature and the grantor's authority before accepting a write to <code>auth/</code>.</li> </ol> <p>Path-content consistency: For critical auth paths (role grants, tool definitions), the server verifies that the data content matches the path. A tool with write access to <code>auth/users/U.../roles/member</code> cannot write <code>{\"role_id\": \"admin\"}</code> there \u2014 the data must match the path.</p>"},{"location":"security/internals/#authentication-challenge-response","title":"Authentication: challenge-response","text":"<pre><code>Client \u2192 Server: POST /spaces/{spaceId}/auth/challenge  {public_key: \"U...\"}\nServer \u2192 Client: {challenge: \"&lt;random nonce&gt;\", expires_at: ...}\nClient: signature = Ed25519.sign(challenge.encode(\"utf-8\"), privateKey)\nClient \u2192 Server: POST /spaces/{spaceId}/auth/verify  {public_key, challenge, signature}\nServer: verifies signature; issues JWT\n</code></pre> <p>The JWT is a standard HS256 token signed with the server's <code>jwt_secret</code>. It expires after <code>jwt_expiry_hours</code> (default 24h). The server validates it on every subsequent request.</p> <p>The challenge is a random nonce that expires after <code>challenge_expiry_seconds</code> (default 300s), preventing replay attacks.</p>"},{"location":"security/internals/#opaque-password-based-key-recovery","title":"OPAQUE: password-based key recovery","text":"<p>OPAQUE is an asymmetric PAKE used for credential recovery only \u2014 not for access control.</p> <p>When a user registers with OPAQUE: 1. The client runs the OPAQUE registration protocol with the server, producing an <code>export_key</code> (64 bytes) known only to the client. 2. The client derives a wrapping key: <code>wrap_key = HKDF(export_key, \"reeeductio-credential-wrap\")</code>. 3. The client encrypts <code>privateKey \u2016 symmetricRoot</code> with <code>wrap_key</code> using AES-256-GCM and stores the ciphertext on the server at a known path.</p> <p>When a user logs in with OPAQUE: 1. The client runs the OPAQUE login protocol, recovering the same <code>export_key</code>. 2. The client derives <code>wrap_key</code> and decrypts the stored credentials. 3. The client uses the recovered <code>privateKey</code> to perform the normal Ed25519 challenge-response login.</p> <p>Key properties: - The password is never transmitted to the server (OPAQUE guarantee). - A server compromise reveals only the wrapped credentials \u2014 useless without the password. - Password changes re-wrap the same keypair; the public key and space ID never change. - OPAQUE is rate-limited server-side to slow brute-force attacks.</p>"},{"location":"security/internals/#data-store-security-comparison","title":"Data store security comparison","text":"Store Encrypted Signed Chained Replay-safe Topics (messages) \u2705 \u2705 \u2705 \u2705 State \u2705 \u2705 \u2705 \u2705 Blobs \u2705 n/a n/a (content-addressed) \u2705 Data (KV) \u2705 \u2705 \u274c \u26a0\ufe0f (cached clients only)"},{"location":"security/internals/#related-pages","title":"Related pages","text":"<ul> <li>Security Overview \u2014 plain-language summary</li> <li>Threat Model \u2014 what attacks are in and out of scope</li> </ul>"},{"location":"security/overview/","title":"Security Overview","text":"<p>Not independently audited</p> <p>rEEEductio has been designed with security in mind but has not been independently security-audited. Use it at your own risk for anything sensitive.</p>"},{"location":"security/overview/#what-reeeductio-protects","title":"What rEEEductio protects","text":"<p>rEEEductio is built around one core principle: the server should not be trusted with your data. Every piece of user content is encrypted before it leaves the client. The server stores only ciphertext and the bare minimum plaintext state required for authentication and authorization.</p> Property Mechanism Confidentiality AES-256-GCM encryption, keys derived client-side with HKDF Message integrity SHA-256 hash chains \u2014 any tampering breaks the chain Sender authenticity Ed25519 signatures on every message Access control Capability-based, stored in signed state entries Authentication Ed25519 challenge-response \u2192 short-lived JWT Password login OPAQUE aPAKE \u2014 password never reaches the server"},{"location":"security/overview/#cryptographic-primitives","title":"Cryptographic primitives","text":"Primitive Algorithm Use Signing Ed25519 Identity, message signatures, capability grants Key derivation HKDF-SHA256 Deriving per-topic, per-space encryption keys Symmetric encryption AES-256-GCM Data at rest on the server Hashing SHA-256 Message IDs, blob IDs, chain integrity Password auth OPAQUE Optional password-based credential recovery"},{"location":"security/overview/#what-the-server-can-and-cannot-do","title":"What the server can and cannot do","text":"Action Can the server? Read encrypted message content \u274c No \u2014 stored as AES-GCM ciphertext Read encrypted app state \u274c No \u2014 also encrypted Read plaintext auth state \u2705 Yes \u2014 required for authentication and authorization Read encrypted blob content \u274c No \u2014 also encrypted Forge a message \u274c No \u2014 messages require a valid Ed25519 signature Reorder messages in a topic \u26a0\ufe0f Partially \u2014 the server controls <code>server_timestamp</code>, but clients verify the <code>prev_hash</code> chain Drop messages \u26a0\ufe0f Yes \u2014 a malicious server can withhold messages, but clients detect gaps in the chain Replay old messages \u26a0\ufe0f Partially \u2014 timestamps and chain links make this detectable Deny access to data \u2705 Yes \u2014 the server decides whether to respond to requests Learn who communicates with whom \u26a0\ufe0f Partially \u2014 the server sees which user IDs authenticate to each space and when, but user identities cannot be linked across spaces"},{"location":"security/overview/#data-store-security-notes","title":"Data store security notes","text":"<p>Not all storage primitives have identical security guarantees:</p> <p>Topics (message chains): Strongest guarantee. Every message is signed and chained. A server that substitutes or reorders messages will be detected by chain validation.</p> <p>State: Strong guarantee. State is backed by the message chain, so the full history is verifiable. The current value is the latest signed write.</p> <p>Blobs: Content-addressed. The blob ID is derived from the encrypted content hash, so the server cannot substitute different content for the same ID without detection.</p> <p>Data (KV store): Weaker. Each entry is signed, but there is no chain. A compromised server can replay an older value for a key without detection by a client that hasn't previously cached the newer value. For security-sensitive metadata, use State instead of Data.</p>"},{"location":"security/overview/#known-limitations","title":"Known limitations","text":"<ul> <li>No forward secrecy: If the <code>symmetricRoot</code> is compromised, all past and future messages in the space can be decrypted. Key rotation requires migrating to a new space.</li> <li>Server sees metadata: The server knows which space IDs exist, which user IDs authenticate, and when. It does not see content, but it sees traffic patterns.</li> <li>Server can withhold messages: There is no cryptographic proof that the server delivered all messages. A malicious server can withhold or delay them; clients can detect gaps but cannot force delivery.</li> <li>No independent audit: This project has not been reviewed by an independent security firm.</li> </ul>"},{"location":"security/overview/#related-pages","title":"Related pages","text":"<ul> <li>Threat Model \u2014 what attacks are in scope and out of scope</li> <li>Security Internals \u2014 detailed cryptographic design for security-curious developers</li> </ul>"},{"location":"security/threat-model/","title":"Threat Model","text":"<p>This page describes what rEEEductio is designed to protect against, what it explicitly does not protect against, and the honest status of its security review.</p>"},{"location":"security/threat-model/#principals","title":"Principals","text":"Principal Description Space creator Holds the space's Ed25519 private key; has full implicit access to the space Space member A user or tool with explicitly granted capabilities Server operator Runs the rEEEductio server and has access to the database and object store Network attacker Can observe or intercept traffic between client and server Passive third party Can read data that leaks outside the system (e.g. backups, logs)"},{"location":"security/threat-model/#in-scope-threats-we-aim-to-prevent","title":"In scope \u2014 threats we aim to prevent","text":""},{"location":"security/threat-model/#honest-but-curious-server","title":"Honest-but-curious server","text":"<p>A server that follows the protocol correctly but reads everything it can.</p> <ul> <li>Message content: Server sees only AES-GCM ciphertext. Without the <code>symmetricRoot</code>, content cannot be decrypted.</li> <li>State content: Same \u2014 encrypted at rest.</li> <li>Blob content: Encrypted with a per-blob DEK. The server stores ciphertext only.</li> <li>Forgery: The server cannot forge messages or state entries because it doesn't have users' Ed25519 private keys.</li> </ul>"},{"location":"security/threat-model/#message-tampering","title":"Message tampering","text":"<ul> <li>Each message includes an Ed25519 signature from its sender. A modified message will have an invalid signature.</li> <li>Each message includes <code>prev_hash</code> \u2014 the SHA-256 hash of the previous message. Inserting, deleting, or reordering messages breaks the chain and is detected by clients.</li> </ul>"},{"location":"security/threat-model/#unauthorized-access","title":"Unauthorized access","text":"<ul> <li>Non-members cannot authenticate (the challenge-response requires an Ed25519 private key that only space members hold).</li> <li>Members are limited to their granted capabilities; the server enforces these against the signed capability entries in the space's state.</li> </ul>"},{"location":"security/threat-model/#privilege-escalation-through-tool-creation","title":"Privilege escalation through tool creation","text":"<ul> <li>A tool cannot be granted capabilities its creator doesn't have. The server enforces that the grantor's permissions are a superset of what they are granting.</li> <li>Once created and granted powers, a tool can grant capabilities and roles that it itself does not have. Be very careful when creating tools and when sharing their private keys.</li> </ul>"},{"location":"security/threat-model/#password-interception-opaque","title":"Password interception (OPAQUE)","text":"<ul> <li>When OPAQUE is enabled, passwords are never transmitted to the server. The OPAQUE protocol is designed so the server cannot recover the password even with a full database dump.</li> </ul>"},{"location":"security/threat-model/#partially-in-scope-detectable-but-not-fully-preventable","title":"Partially in scope \u2014 detectable but not fully preventable","text":""},{"location":"security/threat-model/#message-withholding","title":"Message withholding","text":"<p>A malicious server can refuse to deliver messages. Clients cannot force delivery. However: - The hash chain means clients that do receive messages can verify they form a contiguous, unforked sequence. - A gap between the last cached message and new messages is detectable.</p>"},{"location":"security/threat-model/#message-replay-state-and-data","title":"Message replay (state and data)","text":"<ul> <li>State: The full history is in the chain; clients can detect if the server presents an older state as current, because newer signed entries would be missing.</li> <li>Data (KV): A compromised server can silently replay an old value for a key to a client that has never seen the newer value. This is a known weakness \u2014 use State for security-sensitive data.</li> </ul>"},{"location":"security/threat-model/#metadata","title":"Metadata","text":"<p>The server learns: which space IDs exist, which user IDs authenticate, when they connect, and approximately how much data is in each topic. This is traffic analysis, not content analysis, but it is real information leakage.</p>"},{"location":"security/threat-model/#out-of-scope-threats-we-do-not-aim-to-prevent","title":"Out of scope \u2014 threats we do not aim to prevent","text":""},{"location":"security/threat-model/#compromised-client","title":"Compromised client","text":"<p>If an attacker controls the client device, they can read plaintext directly. No server-side design can prevent this.</p>"},{"location":"security/threat-model/#compromised-symmetricroot","title":"Compromised <code>symmetricRoot</code>","text":"<p>The <code>symmetricRoot</code> is the root encryption secret. Anyone who has it can decrypt all content in the space \u2014 past and future. Protecting it is the user's responsibility (use OS keychain, secrets manager, OPAQUE for recovery).</p>"},{"location":"security/threat-model/#denial-of-service","title":"Denial of service","text":"<p>The server can be taken offline or overloaded. rEEEductio has no built-in DoS mitigation.</p>"},{"location":"security/threat-model/#physical-access-to-the-server","title":"Physical access to the server","text":"<p>An attacker with physical access to the server hardware or database files sees encrypted content. But combined with the <code>symmetricRoot</code> (from a captured client), they could decrypt everything. Protect the server like any other sensitive infrastructure.</p>"},{"location":"security/threat-model/#anonymity","title":"Anonymity","text":"<p>rEEEductio does not hide who is communicating with whom. For anonymity, route traffic through Tor or a VPN before reaching the rEEEductio server.</p>"},{"location":"security/threat-model/#supply-chain-attacks","title":"Supply chain attacks","text":"<p>rEEEductio depends on third-party libraries (<code>@noble/ed25519</code>, <code>@noble/ciphers</code>, <code>@noble/hashes</code>, <code>@serenity-kit/opaque</code>, etc.). Compromised dependencies could undermine all security guarantees. Use dependency pinning and integrity checking in production.</p>"},{"location":"security/threat-model/#audit-status","title":"Audit status","text":"Area Status Cryptographic design Self-reviewed; not independently audited Implementation Self-reviewed; no formal code review Dependencies Reputable libraries; no formal audit of dependencies Penetration testing None <p>Recommendation: Do not rely on rEEEductio for life-critical or legally regulated data without an independent security review.</p>"},{"location":"security/threat-model/#related-pages","title":"Related pages","text":"<ul> <li>Security Overview \u2014 what the system protects</li> <li>Security Internals \u2014 cryptographic design details</li> </ul>"}]}